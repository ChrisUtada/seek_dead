# 老虎机对决 · 数值膨胀与策略深度设计框架（极简爽感版 v2.1）

> 状态：**MVP 已落地，已提交 origin/master（最新 `2aab649`）**（详见 §15 执行步骤）。本版为 **v2.1**，在 v2.0 极简爽感收敛版基础上回写两项落地后的重大机制变更（**方案 A 实体转轮带**、**整备槽位收紧**），并撤销一条已失效的旧结论（REELS 3→4）。
>   
> 关联：`docs/分类重构方案.md`（Phase A–D 已落地）、`docs/[已完成]UI_最佳实践改造方案.md`（控制器/UI 分离已落地）、`docs/[已完成]物品分类职责划分.md`（三分类职责宪章 + 来源标签 + 落位表）。
>
> **两条核心诉求（用户拍板）**：
>
> 1. **带来数值爆炸的爽感**。
> 2. **玩法和计算不要过于复杂**。
>
> **v2.0 相对 v1.0 的收敛**：从约 10 个互交系统砍到约 5 个。**砍掉**：塔罗、星球、套装、双货币、逐符号复杂元素判定、属性抗性（进阶）。**保留**：老虎机机制、武器元素化、克制奖罚并存（温和）、护符全局乘区、ante 难度曲线、局内商店。理由见 §2、§13。
>
> **v2.1 相对 v2.0 的变更**：
>
> 1. 新增 **§4 转轮交互模型**（方案 A：实体转轮带 + 玩家手动停）——把"后台加权随机抽"改为"权重 = 带子格子数 + 停止时机"，新增技能层。
> 2. 新增 **§7 整备槽位**——确立「**进池 vs 不进池**」分类原则，槽位由 8 收紧至 5。
> 3. **撤销 REELS 3→4**（原 §8.3）——该条目是旧随机模型下的结论，与方案 A 冲突，移入冻结清单（§13）。
> 4. 新增 **§15 执行步骤**——完成度台账 + 分批路线图，取代原 §13 实施路线。
> 5. **「增益(buff)」槽位改名为「技能(skill)」**（2026-08-03 落地，提交 `161bad0`）：机制照搬（进轮、初期限 1），原 4 个增益拆分——`haste`/`rage` 留作技能，`bulwark`/`regen` 转护符（新增 `heal` 护符被动）。详见 `docs/[已完成]物品分类职责划分.md`。下文所有「增益」措辞已统一为「技能」。
>
> **当前代码事实**（落地基准，2026-08-02 复核）：
>
> - 转轮 `REELS = 3`、`ROWS = 1`（3 列单行，一条赔付线）。
> - 转轮为**实体带子**：`reel_strips[r]` = Array[[SymbolData, element]]，按权重展开成格子后洗牌；`_STRIP_MIN_CELLS = 30`；`_SPIN_BASE_WAIT 0.09 → _SPIN_MIN_WAIT 0.035` 加速；**无自动停**。
> - 槽位（四类各自独立、互不算总）：**初始配额 `SLOT_INIT` = 武器2·技能1·消耗品1·护符1**（合计 5 仅为初始值之和，非共享闸门）；**天花板按「进池与否」分野——进池类（武器/技能）`UNCAPPED` 无上限**（转轮稀释自身即刹车 + 金币递增价），**不进池类硬限** `CONSUMABLE_CAP=2 / CHARM_CAP=3`；成长统一走「**买即开槽**」。**无全局总槽概念**（已移除 v2.1 的 `TOTAL_MAX=5` 共享闸门）。
> - 结算：`_evaluate` 按「符号 + 有效元素」计数后 `_contribute(s, c, acc, elem)` 逐符号累加；**`c = 1` 也照常结算伤害**（非 payline 匹配），`special` 仅在 `c >= 3` 时额外追加一次 base。
> - 克制：`element_counter.gd` `MULT_ADVANTAGE = 1.5` / `MULT_RESIST = 0.85` / 无关 1.0。
> - ante：按「幕间台阶 × 幕内爬升」两段缩放（`_start_room` 内）。`ANTE_ACT_STEP_HP=1.75`/`ATK=1.46`（每进一幕 ×）、`ANTE_ROOM_STEP_HP=1.15`/`ATK=1.10`（同幕每房 ×）。数学等价于原 `1.15^idx`，但解耦两旋钮且对幕内房数变化稳健；boss 永远幕内位置=3=天然峰值（2026-08-09 房数重排后为 21 房/幕内 7 房，boss 幕内位置=6，ante 数值待 F6 复核）。数值保留 F6 验证手感（幕1/2/3 BOSS 缩放后 HP≈266/771/2140）。
> - **已有跨局货币**：`meta["anvil_points"]`（铁砧点数，持久化于 `SaveSystem.lobby_data["anvil_meta"]`）；`meta["weapon_upgrades"]` 按 `resource_path` 给武器主符号加权重。
> - **已有运行时加成范式**：`run_symbol_bonus`（按 `resource_path` 索引的额外权重，局内读表叠加，**不改共享 .tres**）。
> - **已有携带/充能范式**：`consumable_slots`（固定 4 格腰带，每格 {path,item_id,charges,uid} 独立实例；整备（初始）仅选 1 类即占 1 格，商店可重复购买同类补满其余 3 格；用掉即消耗、不每房回满）。
> - `RoomData` 为单敌单元素（name/hp/atk/jam/lock/chaos/heavy/element 各一份），当前仅 4 个 .tres。
> - F-0 属性聚合层已落地：`_agg_power_flat / _agg_shield / _agg_regen / _agg_damage_mult / _agg_symbol_weight_mod`（词缀已废弃，`_affix_*` 中性占位已于死代码清理中删除）。

---

## 0. 一句话设计

老虎机 + build 策略 + 数值膨胀。**爽感来自"看得懂的大数字"，策略来自"转轮之前的 build 决策"，复杂度靠"砍平行系统、呈现层降负"压住。**

---

## 1. 核心矛盾与解法（本框架的根基）

**矛盾**：小丑牌(Balatro)的乘区爆炸之所以成立，是因为玩家**完全掌控出什么牌**（组牌 + 自己把手牌打出去）。但老虎机是**纯随机出符号**——玩家对"这一转转出什么"几乎零控制。

**若照搬小丑牌"战斗内多乘区"**：伤害 = 随机符号 × 一堆看不出哪个生效的乘区 = 黑箱。策略感从"我怎么出牌"降级成"我堆了哪些 buff"，**反而削弱**老虎机。

**解法（两条）**：

- **策略放在 build 层（转轮之前）**：决策发生在整备、商店、升级；战斗内是决策的兑现（纯随机连线爽感）。
- **膨胀长在 build 层**：数值爆炸来自"武器升级（线性）× 护符（乘法）"的积累，不来自战斗内的多步计算。

这条是整个 v2.0 的基石，所有取舍都由此推出。

---

## 2. 设计原则

沿用 `分类重构方案.md` 的「数据即 Resource / 无全局注册表 / 文件夹自动发现 / 表现逻辑分离」，并新增：

- **P0 爽感可解**：每个大数字都能分解出"为什么这么大"（伤害分解显示，见 §5）。
- **P1 策略在 build 层**：决策在整备/商店/升级，战斗内是兑现。
- **P2 温和奖罚**：保留"带对克 ×1.5 / 带错抗 ×0.85"的真实相克（更有深度、更真实），但**幅度温和、从敌人视角呈现（弱点/抗性）**，不背环、不崩盘（见 §6.2）。
- **P3 少而强的乘区**：乘区数量少（3 个），每个都直观（连线/护符/克制）。
- **P4 双轨膨胀**：线性（武器升级）× 乘法（护符），两条线玩家算得过来。
- **P5 取舍即策略**：策略性来自有意义的取舍（如武器槽稀缺 vs 元素覆盖），**不是机制数量**。计算负担 ≠ 决策质量。
- **P6 敌我同步膨胀（ante）**：玩家变强，怪物同步变强，否则中期秒怪、游戏提前结束。
- **P7 禁 mutate 共享 .tres**：局内改动一律走"按 `resource_path` 的运行时加成表"（复用 `run_symbol_bonus` 范式），绝不改共享资源。
- **P8 内容广度撑长尾**：长线新鲜感靠更多武器/护符/敌人/房间，**不靠再加系统**。
- **P9 技能层归玩家**（v2.1 新增）：转轮结果由玩家按停时机决定，不由后台 RNG 代劳。凡"玩家能操作的环节"，不设自动兜底。
- **P10 槽位按"是否进池"管控**（v2.1 新增）：进转轮池的（武器/技能）稀释带子、影响手感与克制浓度，必须严格限量；不进池的（消耗品/护符）只占界面，可宽松。**不按数量一刀切。**

---

## 3. 核心循环

```
整备(build) → 转轮(随机连线爽) → 结算(伤害分解) → 奖励(金币/掉落)
     ↑                                                  ↓
     ←──────────  商店(投资) ← 下一房(更深更难/ante)  ←
```

- **策略**发生在：整备（选武器/符号池）、商店（投资配比）、升级（线性 vs 乘法）。
- **技能**发生在：转轮（按停时机，见 §4）。
- **爽感**发生在：转轮（连线命中）、结算（大数字 + 分解）。

---

## 4. 转轮交互模型（方案 A：实体转轮带 + 玩家手动停）

> v2.1 新增。**已落地**，取代原先的"后台加权随机一次抽完"。

### 4.1 为什么改

原模型是：按 SPIN → 后台按权重抽一次随机数 → 直接出结果。玩家零操作空间，结果 100% 由 RNG 决定。这导致"权重"在玩家眼里像一张**被后台操控的不透明概率表**，而不是一台看得见的老虎机。

真实老虎机的"权重"本质就是**转轮带上该符号的格子数**（strip composition）。整备界面的「🔥×5 ✕×3 🗑️×2」本就该读作"火格占 5/10、交叉 3/10、废铁 2/10"——概率与带子长度一一对应，权重本身没有问题，**缺的是玩家的操作环节**。

### 4.2 模型定义

| 概念       | 实现                                              | 说明                                                     |
| -------- | ----------------------------------------------- | ------------------------------------------------------ |
| **转轮带**  | `reel_strips[r]` = Array[[SymbolData, element]] | 由合并符号池按权重展开（权重 5 = 带子上 5 格）后洗牌，每列一份副本（同构成、异顺序）         |
| **最小带长** | `_STRIP_MIN_CELLS = 30`                         | 带子过短时**整份平铺**补足，只拉长视觉周期，**各符号占比、命中概率完全不变**             |
| **旋转**   | `_spin_timer` 节拍器，每跳推进 `reel_cursor[r]`         | 加速 `0.09s → 0.035s`（约 37 跳封顶，≈28 格/秒）                  |
| **停止**   | 玩家操作，落点 = 按停瞬间的 `reel_cursor`                   | **无自动兜底，不停就一直转**                                       |
| **落定**   | `emit_signal("spin_finished")`                  | `_on_spin_pressed` / `_free_spin` await 后再 `_evaluate` |

**三条停止入口**（均不会被禁用，无卡死风险）：

| 入口      | 行为                         |
| ------- | -------------------------- |
| SPIN 按钮 | 旋转中 = 停下**下一列**            |
| 空格键     | 同上（仅整备界面 `in_loadout` 时屏蔽） |
| 点击某一列   | **单独锁定**该列                 |

### 4.3 设计意义

- **难度来源迁移**：从"后台概率"变成"转速 + 你的眼力手速"。越转越快 = 越难精准停在目标符号上。
- **权重语义澄清**：权重不再是黑箱概率，而是"带子上看得见的格子数"。稀有强力符号照旧稀有（格子少），但玩家有主动权去博它。
- **与 build 层耦合**：带子长度 = 所有携带武器的权重总和。**带的武器越多，目标符号密度越低、越难按停**——这让 §7 的槽位收紧有了直接的手感反馈（见 §12.1）。
- **敌人干扰意图仍生效**：注废（jam）/ 锁轮（lock）在 `_lock_reel` 的瞬间覆盖结果，玩法不变。

### 4.4 调参入口

| 常量                 | 当前值        | 效果                                    |
| ------------------ | ---------- | ------------------------------------- |
| `_SPIN_BASE_WAIT`  | 0.09       | 起始每跳间隔，调大 = 起手更慢更好停                   |
| `_SPIN_MIN_WAIT`   | 0.035      | 最高速封顶，**调大 = 变慢好停，调小 = 变快难停**（主要难度旋钮） |
| 加速斜率               | 0.0015 / 跳 | 调大 = 更快进入高速                           |
| `_STRIP_MIN_CELLS` | 30         | 仅影响视觉重复周期，不影响概率                       |

---

## 5. 战斗结算模型（3 乘区 + 伤害分解）

### 5.1 三个乘区（少而强）

**单符号伤害 = base × 连线数 × 护符 × 克制**

| 乘区      | 来源     | 玩家直觉                              | 现状                                              |
| ------- | ------ | --------------------------------- | ----------------------------------------------- |
| **连线数** | 老虎机原生  | 连得越多越痛                            | ✅ 已有（`mult` = 同符号计数）                            |
| **护符**  | 收集/商店  | 护符越多全局越痛（收集爽）                     | ✅ 已落地（`charm_damage_mult` → `_agg_damage_mult`） |
| **克制**  | 带对元素武器 | 弱点 ×1.5 / 抗性 ×0.85 / 无关 ×1.0（策略点） | ✅ 已落地（`element_counter.gd`）                     |

**回合总伤 = Σ 各落线符号伤害**（沿用 `_evaluate` 逐符号累加结构，改动最小）。

> ⚠ **易误判的机制事实**：结算**不要求凑三连**。`_contribute(s, c, ...)` 中 `c = 1` 也照常出伤害，`special` 仅在 `c >= 3` 时额外追加一次 base。因此"多带武器"不会让玩家打空——**它的代价是隐形的**（见 §7.2）。这一点直接决定了槽位该怎么管。

### 5.2 伤害分解显示（P0 的落地）

伤害数字旁给分解，让玩家**爽得明白**：

```
符号[火·剑]  8 × 连线3 × 护符2 × 克制1.5 = 72
```

> 爽感的一半来自"看懂这一下为什么这么大"。这比堆更多系统更划算。同时是 ante 调参的 debug 面板。

> 📌 **2026-08-07 呈现层变更**：固定中栏伤害分解面板已移除——现为**敌人头顶多行飘字**（`battle_hud._float_breakdown`，`d8ae32f`），逐符号明细改为**编辑器控制台调试日志**（`1b8c6e8` / `5264da5`，战斗界面滚动日志框一并移除）。下方 §5.2 其余内容为机制语义，仍有效。

---

## 6. 武器系统（元素化 + 克制奖罚并存·温和）

### 6.1 武器元素化

- `WeaponData` 加 `@export var element = "none"`（火/冰/毒/光/暗/无）。
- 符号自身 `element == "none"` 则继承武器 `element`；**着色 = 元素**（火=红、冰=蓝…），玩家一眼认出"哪个符号吃加成"。
- 开局武器 = `element="none"`（中性，稳定 ×1.0，教程/兜底/对冲）；属性武器为后期掉落。
- **攻击力差异化（资源化，每把武器专属符号，已落地）**：每把武器的伤害符号是**各自独立的 .tres**、base 不同，而非共用 `slash.tres`（原固定 base=10）。例：战斧 `axe_slash` 14 / 铁剑 `iron_slash` 11 / 火焰剑 `fire_slash` 10 / 冰枪 `ice_shot` 9 / 手枪 `pistol_shot` 8 / 匕首 `dagger_slash` 7，火焰法杖 `flame_special` 25。结算 `_contribute` 用 `sym.base + 全局bonus`，故每把武器的斩/射有各自攻击力；差异围绕 10 浮动，靠废铁权重/出现频率自然平衡（见 §7.2）。毒匕首无直接伤害符号，攻击力走 `poison` 层伤（设计特性）。

### 6.2 克制奖罚并存（温和，P2）

- **弱点 ×1.5，抗性 ×0.85，无关/中性 ×1.0。**
- 带对元素（命中敌人弱点）→ 爆发爽点；带错元素（撞上敌人抗性）→ 轻微吃亏，但**不崩盘、有退路**（中性武器恒 ×1.0）。

**为什么保留惩罚（而非只奖不罚）**：惩罚带来不可替代的价值——

- **真实感**：元素相克本就该"被克变弱"，去掉惩罚则元素退化成单向增益。
- **更深策略**：带对 ×1.5 vs 带错 ×0.85 是**真实风险博弈**（赌克制爆发 vs 保中性稳），优于"贪心覆盖多元素"的浅决策。
- **中性武器价值回归**：恒 ×1.0 成为风险对冲工具（只奖不罚下它毫无意义）。
- **抑制"全元素覆盖流"无脑化**：凑齐元素即稳的困境被打破。

**怎么让惩罚不爆炸复杂度**（三件套控复杂度）：

- **幅度温和**：×0.85 而非 ×0.7，无"带错就崩"的挫败。
- **敌人视角呈现**：显示「弱 火 ×1.5」「抗 毒 ×0.85」，玩家从敌人角度判断，不用背五元环。
- **中性常驻对冲**：永远有 ×1.0 退路，不会"被迫吃惩罚"。
- 伤害分解面板让"为什么 ×0.85"透明可学。

### 6.3 用"敌人弱点/抗性"呈现替代背环（呈现层大简化）

- 底层保留克制关系数据（决定哪敌人弱什么、抗什么），但**呈现给玩家的是敌人视角的"弱点/抗性"而非全局克制环**。
- 敌人显示「弱 火（你打它 ×1.5）」「抗 毒（你打它 ×0.85）」。玩家从敌人角度判断"这怪我打它什么好、什么差"，**零记忆负担**。
- 不用记"火克冰、冰克毒…"的传递环；伤害分解面板让倍率透明可学。

### 6.4 武器槽

- 当前武器**初始配额 2**（开局免费配给；商店「买即开槽」无限扩槽，**无硬天花板**）：带越多废铁越多/按停越难，稀释效应自身即刹车，金币价随已持数递增为第二道闸门。详见 §7。

---

## 7. 整备槽位（进池 vs 不进池）

> 槽位模型（当前代码事实）。**已落地**：四类各自占独立槽位，互不算总、不共享上限——v2.1 曾引入的全局 `TOTAL_MAX=5` 共享闸门已**移除**（会导致"带满护符即锁死武器"的误判），改回每类独立上限。

### 7.1 分类原则（P10）

槽位不按"总数多少"管，按**是否把符号塞进转轮带**管——这是两把完全不同的尺子：

| 类别       | 初始 | 天花板 | 进池？   | 影响                   | 管控尺度                     |
| -------- | -- | --- | ----- | -------------------- | ------------------------ |
| **武器**   | 2  | **`UNCAPPED` 无上限**  | ✅ 进池  | 稀释带子、摊薄克制浓度、拉低目标符号密度 | 稀释自身即刹车 + 递增价（+5/件）      |
| **技能** | 1  | **`UNCAPPED` 无上限**  | ✅ 进池  | 同上，挤占转轮带最凶           | 同上，步进更陡（+6/件）           |
| **消耗品**  | 1  | `CONSUMABLE_CAP=4`（固定腰带，不成长） | ❌ 不进池 | 仅占界面与心智              | 硬限（无自然刹车）                |
| **护符**   | 1  | `CHARM_CAP=3`      | ❌ 不进池 | 唯一收集乘区，是膨胀主引擎        | **硬限**（零代价纯收益，步进价最贵）     |

**天花板按「进池与否」分野，不按类别拍脑袋**：

- **进池类（武器 / 技能）→ 无天花板**。它们的符号挤进同一条转轮带，带越多则废铁占比升高、目标符号命中率被稀释、克制浓度摊薄、按停难度上升——**稀释效应本身就是刹车**，人为封顶属于重复收费。约束交给「金币线性递增价 + 稀释代价」双闸门即可。实际可带数还受**内容池大小**天然限制（武器池 8 / 技能池 2，且同一件不可重复购买）。
- **不进池类（消耗品 / 护符）→ 硬天花板**。它们不进转轮、零稀释代价、**没有任何自然刹车**，尤其护符是「唯一收集乘区」纯收益越多越强，必须硬限量（4 / 3），否则乘区无限叠加直接崩坏数值。

**四类各自独立槽位、互不算总**：初始配额之和 = 2+1+1+1 = 5，但这**只是初始值之和，不是共享闸门**——任一类满槽绝不影响其他类。取舍张力来自**各自内部**（越扩越贵、稀释代价），而非跨类合计，故不存在"总槽已满"导致别类被锁死的误判。

> 📝 **概念区分（重要）**：本文「技能」一词有两义——(1) **v2.1「按停技能层」**（§4 / §12.1）：玩家手动停转轮的操作技巧，属交互层；(2) **物品分类「技能(skill)」槽位**：即原「增益(buff)」改名而来（进轮、初期限 1、机制照搬），属 build 层。§7 槽位表与 §15 台账中的「技能」均指后者（物品分类）。

### 7.2 多带武器的三条隐形代价

因为 `c = 1` 也结算伤害（§5.1），多带武器**不会让玩家打空**，坏处全部隐形。而**一个没有可感知代价的选项等于没有选项**，玩家会一直带满——这比"带满就废"更糟。三条代价：

1. **克制乘区吃不满**。带 1 把火剑 → 三格大概率全火 → 全吃 ×1.5；带 5 把杂武器 → 各元素混杂 → 有 ×1.5 有 ×0.85，期望被拉回 ×1.0。精心设计的克制系统被玩家自己稀释掉。
2. **`special` 的 ×3 爆发几乎不触发**。这是武器的"必杀感"来源，带满等于主动放弃。
3. **带子变长、按停变难**（方案 A 落地后新增）。1 把武器 ≈ 10 格（火占 50%），5 把 ≈ 50 格（火占 10%）。想按停在火格上，等待时间变成 5 倍——**刚做完的技能层被"带满"这个选项废掉**。

### 7.3 成长规划

**统一成长机制「买即开槽」**：在商店购买某类物品时，若该类当前槽位已满且未触天花板，本次购买**直接把该类上限 +1**（不单独售卖抽象的"槽扩展"商品）。节奏闸门 = 同类价格随已持数递增，且**加价从「首次扩槽」那一件起算**（填初始空位仍是原价）：

| 槽位  | 初始 | 天花板 | 步进  | 价格曲线（去随机项）      | 备注                            |
| --- | -- | --- | --- | --------------- | ----------------------------- |
| 武器  | 2  | **∞** | +5  | 8 · 8 · 13 · 18 · 23 · 28 … | 无硬顶；带越多废铁越多/按停越难即自然刹车，递增价为第二道闸门 |
| 技能  | 1  | **∞** | +6  | 6 · 12 · 18 · 24 …       | 无硬顶；进池挤占转轮带最凶故步进最陡          |
| 护符  | 1  | 3   | +8  | 10 · 18 · 26      | 不进池、零代价故须硬顶；唯一收集乘区，步进最贵；混合护符负面为未来第三道刹车 |
| 消耗品 | 1  | 4   | +4  | 5 · 9            | 不进池，仅界面负担，固定腰带（可重复购买同类）                 |

---

## 8. 防御系统（单一护盾资源）

- **职责分离**：防御全部归入「护盾」单一资源，由护符/奖励/技能按不同时机灌入（满足"武器进攻、护符提供防御"）。**不另设独立护甲数值**——避免"两个平减数字"的重复感（经典做法参考小丑牌/杀戮尖塔的单一防御资源）。⚠ **澄清（2026-08-03）**：上述"不另设护甲"指**玩家**防御资源，避免玩家侧出现第二个平减数字；**敌人护甲（扁平池，`RoomData.armor` → `enemy_armor`/`enemy_armor_max`）是敌方属性、属 RPG 破甲机制，与玩家护盾不冲突**，按用户诉求新增（见 §10.6）。
- **护盾三来源、两种节奏**：
  - 开局爆发：`ward_charm`（`room_shield`，每房开局一次性灌入）、`守望结界`奖励（`run_shield_next`）。
  - 每回合涓流：`guard_charm`（`shield`，每回合 +N，走 `_agg_shield()`，与铁壁护符同通道）——挨打才消耗、不挨打则累积，等效"常驻减伤"手感。
- **受伤公式**：`受伤 = max(0, raw − shield)`（护盾会耗尽，每房刷新；涓流每回合补）。**最简单、不背环**，与"克制用敌人视角呈现"的简化精神一致。
- 代码：`charm_shield_trickle` 聚合（镜像 `charm_room_shield`）→ `_agg_shield()` 在 `_evaluate` 阶段 1 落地；`_enemy_deal_damage` 仅消费 `player_shield`。`player_armor / charm_armor / _agg_armor / _affix_armor` 已全部删除。
- **属性抗性（fire_def…）：冻结**（进阶可选；要加再说，见 §13）。`StatTarget` 预留的 `FIRE_DEF…` 继续作死代码，本期不启用。

---

## 9. 护符系统（唯一收集乘区）

- 护符 = **全局乘区 ×N**，是数值爆炸的主引擎；堆叠产生复利爽感。
- 槽位：**初始 1、硬天花板 `CHARM_CAP=3`**（把乘区数量约束在 3 内便于平衡，低于 Balatro 的 5）。护符不进池、零稀释代价、纯收益，**没有任何自然刹车**，故与进池类（武器/技能无上限）不同，必须硬限。经商店「买即开槽」成长，步进价最贵（10→18→26）以保稀缺。未来混合护符（正负并存）的负面为第三道刹车（§7.3）。护符上限独立，不因他类占满而受影响。
- 部分护符兼作防御来源（给 shield：如 `ward_charm` 开局爆发、`guard_charm` 每回合涓流）。
- 代码：✅ 已落地——`ItemData` 加 `mult_value` 字段，`_apply_charms` 的 `damage_mult` 分支累乘进 `charm_damage_mult`，由 `_agg_damage_mult()` 在回合层结算（属全局乘区）。示例资源 `resources/charms/rage_charm.tres`（×1.5）。

---

## 10. 数值膨胀（双轨 + ante）

### 10.1 双轨膨胀（P4）

- **武器升级（线性 base，8→20）**：看得见摸得着的成长。沿用铁砧/升级管线（`meta.weapon_upgrades` 按 `resource_path` 加成，**不改共享 .tres**）。
- **护符乘区（乘法 ×N）**：复利爆炸。
- 线性 × 乘法 = 数字自然膨胀，但只有两条线，玩家算得过来（极简版 chips×mult）。

### 10.2 ante 难度曲线（P6，膨胀的必需前提）

- `RoomData` 存**基础值** `base_hp / base_atk`；`_start_room` 按全局已通关房间数 `n` 缩放：
  - 推荐（平滑）：`hp = base_hp × (1+α)^n`，`atk = base_atk × (1+β)^n`。
  - 初值参考：α_hp ≈ 0.15、β_atk ≈ 0.10（**需真机调**）。
- **run 结构（roguelike 骨架，已落地 S10 T3；2026-08-09 房数重排）**：**21 房 = 3 幕 ×（4 普通 + 2 精英 + 1 BOSS）**，每局 `_full_reset` 时从全量 51 间 .tres 池按幕抽取（每幕 4 普通 + 2 精英 + BOSS 4 选 1，BOSS 池随内容补齐至 12）。通关**最终** BOSS（最后一间，非任意 BOSS 房）获胜 → 元进度三选一（见 §10.5）。单局约 40–50 分钟（手动停 3 列 × 21 房）。
- **敌方必须同步指数增长**，否则玩家爆炸后无怪可打。

### 10.3 转轮列数 ~~REELS 3→4~~（v2.1 已撤销·移入冻结清单）

> ❌ **此条目已失效，勿按旧路线实施。** 保留记录以防重复提案。

**撤销理由**：原条目写于"后台加权随机一次抽完"的旧模型下——那时加一列的操作成本为零，伤害期望直接 ×4/3，确实是白捡的杠杆。方案 A（§4）落地后，**列数从"数值参数"变成了"节奏参数"**，每一列都要玩家亲手按停，第 4 列 = 每回合操作量 +33%。这个代价在写原条目时根本不存在。

三条具体损失：

1. **节奏被稀释**：老虎机的爽点是"短促紧张 → 立刻见结果"，按停链从 3 拉到 4，紧张感摊薄成疲劳感。
2. **`special` 的爆发阈值恰好绑定 3 列**：`c >= 3` 即三连即满屏，是全局唯一的"完美一击"瞬间。4 列后三连只占 3/4，满屏仪式感消失；阈值改 4 又几乎打不出来。这个数值美感是 3 列白送的。
3. **与槽位收紧（§7）直接打架**：武器砍到 2 把就是为了抬高目标符号密度、让按停变好按；加一列等于要求玩家在同一难度下多做一次正确判断，两个改动方向相反。

**唯一支持理由（策略深度）在当前代码下不成立**：结算是逐符号独立计数（`c = 1` 也出伤害），不是 payline 匹配。第 4 格产生的不是新组合，只是**第 4 份独立加法伤害**——策略深度一分没涨，纯加数值。

**若确需更多格子**：`ROWS 1→2` 优于加列——一次按停同时定下 2 格，**格子 +100% 而操作次数不变**。代价是同屏信息量翻倍、`_evaluate` 计数需覆盖多行、界面更挤。同属"可选、非必须"。

**回头重估的判据**（真机 3 列跑顺后）：

- 反馈"还没爽到回合就结束了 / 单回合决策太单薄" → 加列对症，可重议；
- 反馈"按停三次已经手忙脚乱" → **永久否决**；
- 若将来把结算改成"必须连成线才算"，多一列会真正改变组合空间，届时需完全重新评估。

### 10.4 局内爆发杠杆：special 三连「连锁重触发」（✅ 已落地 `afcfb29`）

**要解决的问题**：局内所有乘区都被刻意剥离（防黑箱），导致每回合伤害是一条平线；到 BOSS 阶段 build 已定型，只剩"再打十几回合"的机械感，**没有任何单回合的数值尖峰**。

**机制**（Balatro 重触发思路，但保持可读）：
- 打出 `special` 三连 → **免费重转一次**，且**只有 special 部分**吃连锁倍率 `CHAIN_STEP = 1.5`；
- 连锁可累计：第 n 次重转 special 倍率 = `1.5^n`；
- 上限 `CHAIN_MAX = 4`（=1 次正常 + 3 次免费重转），理论峰值 special ×3.375；
- 敌人被打死立即中断连锁；伤害分解尾部显示 `连锁×N`，日志 + 飘字实时提示。

**为什么不算黑箱**：倍率只作用在**玩家亲手按停凑出的 special 三连**上，因果链是"我按准了 → 立刻多一次机会"，不是后台暗算。这是**唯一的局内乘区**，其余全部留在 build 层。

### 10.5 跨局爆发杠杆：每局结束元进度三选一（✅ 已落地 `4afacf2`；真·三选一 + 收尾 BUG 修复见 `333717a`）

> ⚠️ **2026-08-07 退役**：双轨元进度旋钮（`meta.weapon_base_bonus` / `meta.charm_upgrades`）已随铁砧纯 gacha 定案**全链路删除**（`5ed5d1f`，D10 死键清理；见《商店升级重构_执行方案》§6）。当前 `roll_meta_choices()`（`reward_system.gd`）候选仅剩「铁砧点数」兜底一项，本节以下均为**历史记录，不再生效**。

**双轨在 meta 层的具体承载**（对应 §10.1）：

| 轨 | 载体 | 常量 | 上限 |
|---|---|---|---|
| 线性 | `meta.weapon_base_bonus[武器路径]` | `WEAPON_BASE_STEP = 3` | 无（受 ante 曲线制衡） |
| 乘法 | `meta.charm_upgrades[护符路径]` | `CHARM_MULT_STEP = 0.12` /级 | 总乘区硬顶 `CHARM_MULT_CAP = 6.0` |

- **触发时机**：**通关整局**（最后一间 BOSS）后弹出，选一项 → `_save_meta()` → `_full_reset()` 开新局。
- **候选构成**：本局携带武器各 1 张（+base）+ **仅乘区型护符**各 1 张（+mult）+ 铁砧点数 1 张兜底 → `shuffle()` 取 `META_CHOICE_COUNT = 3`。
  - ⚠ 非 `damage_mult` 的护符（守备/抗扰/丰沛/守望）**不出卡**——它们没有升级通道，出卡等于假选项。
- **护符乘数化**：锋锐护符由加法 `damage_bonus +1` 改为乘数 `damage_mult ×1.10`，护符线正式成为"唯一收集乘区"（§9）。**护符数量上限仍锁死 3**，膨胀只走"数值增值"，不走"数量增长"——数量不涨才保证乘区可算。
- **与局内连锁叠乘**：武器 base 在 `_contribute` 中于所有乘法**之前**叠加，因此同时被护符乘区与连锁倍率放大 → 跨局成长直接喂给局内爆发。

### 10.6 反制即爆发（Plan C）✅ 已落地：敌人护甲扁平池 + 克制核爆

**用户诉求（2026-08-03）**：既要有"反制即爆发"的特别爆炸解法，又想要 RPG 式敌人护甲——先破甲后掉血，部分符号可穿透护甲。两者一并落地（提交见 §15.1）。

**① 敌人护甲（扁平池，RPG 破甲）**
- `RoomData.armor`：敌人护甲上限（随 ante 缩放 `hp_scale` → `enemy_armor_max`/`enemy_armor`）。9 间房已配值（普通 10–16 / 精英 20–28 / BOSS 20–35）。
- 伤害结算 `_apply_enemy_damage(amount, pierce)`：**非穿透**先扣 `enemy_armor`、溢出进 `enemy_hp`；**穿透**（`pierce=true`）直接扣 `enemy_hp`，不先扣护甲。
- 穿透符号：`shot` / `pistol_shot` / `ice_shot` 的 `symbol_data.pierce_armor = true`（三射击符号），其余符号正常破甲。
- HUD：敌人面板新增「护甲 X/Y」标签（steel 蓝 `Palette.ARMOR`），无护甲敌人隐藏；伤害落地即时刷新（`_refresh_meta`）。

**② 反制即爆发（双触发，Plan C）**
- `special` 三连 = **通用破甲**：清空当前敌人全部护甲，开启「伤害直击 HP」的爆发窗口（所有敌人通用破绽）。
- 克制**元素**三连 = **进阶核爆**：在破甲基础上再追加 `enemy_hp_max × 20%` 的穿透核爆（一次直击，不进护甲）。
- 埋点：`_contribute` 在 `em > 1.0` 且 `raw >= 2` 时置 `acc["counter_triple"]`；`special` 三连置 `acc["special_triple"]`；`_evaluate` 阶段2 末尾 `counter_triple → _on_counter("element")`、`special_triple → _on_counter("special")`。

**③ 为何 Plan C（而非 A/B）**：符号库缺「暗」元素符号（仅 火/冰/毒/光 + special），克光/克火敌人所需的暗三连暂转不出；`special` 三连作**通用兜底破甲**保证任何敌人都能被"反制打开"，克制元素三连再叠核爆作为进阶奖励，二者并存。

**④ 锈蚀傀儡·熔铸护甲（成长型护甲原型）**
- `rust_armor_gimmick.gd`：每 2 回合叠 1 层护甲（`ARMOR_PER_STACK = 8`，上限 `MAX_STACKS = 3` 层，叠加在 `RoomData.armor` 之上），即"先破甲后掉血"的 canonical 示例；旧百分比减伤 `boss_dmg_mult` 已废弃，统一为护甲扁平池。
- 玩家破甲手段：普通伤害破甲 → 下一发直击 HP；或 `shot` 类穿透符号绕过护甲；或 `special` 三连一次清空（熔铸护甲需再等 2 回合叠回）。

---

## 11. 经济与商店

- **局内金币**：击杀/清房获得（清房 +5 / Boss +10），**每局清零**（膨胀留局内，防滚雪球）；起始 4 金。
- **转轮金币符号**：`resources/symbols/gold.tres` 常驻转轮池（权重 `GOLD_POOL_WEIGHT=3.0`，远低于伤害符号，防稀释 DPS），落在线上的金币符号按 `GOLD_PER_COIN=1 × 命中数` 转化为金币、不造成任何伤害。与清房/利息构成「批发 + 零售」双收入，喂给商店（S7）。
- **商店**：房间之间出现（奖励三选一之后），卖武器/护符/消耗品/技能；**刷新随机**（每房 `candidates.shuffle()` 取 6 件，防背公式）。与房奖励三选一并存（前者白送、后者消费，构成经济循环）。购买写入对应 selected 数组，下房 `_start_room` 重建符号池 + 重算护符被动使其生效。
- **消耗品 = 可消耗资源（非自动恢复）**：自 S6–S8 商店落地后，消耗品**不再每房/每局自动回满**——仅在「进入持有」时初始化一次次数（整备确认 / 商店购买），用掉即消耗；商店是补给唯一来源。消耗品采用**固定 4 格腰带模型**：每格是一个独立实例（含自身 `charges`），**允许同类重复占格**（如四格全为血瓶）；**整备（初始）仅能勾选 1 类（初始占 1 格），其余 3 格靠商店购买填充**，腰带总容量恒为 4、不再「买即开槽」成长；用尽的格子移出腰带并释放该格，可于商店再次购入同类补满。此改动让商店对消耗品真正有意义（否则免费每房回满使购买无意义）。
- **同类物品唯一持有（消耗品除外）**：武器/护符/技能购买前校验 `selected_*` 是否已含该资源路径，已拥有则拒绝（"已拥有，无法重复购买"），避免同一护符效果因重复购买而叠加（原 bug：`_apply_charms` 用 `+=` 累加，两张相同护符会使效果翻倍）。**消耗品是例外**——腰带模型允许同类重复占格（如四格全血瓶），商店购买同类只追加一个新腰带格、不拒单；`selected_*` 仅用于整备去重勾选（上限 1 类），不约束商店补给。整备屏因每张卡为独立对象、靠 `card["selected"]` 切换，本就不会重复。
- **利息（S8）**：清房金币发放时并入 `min(gold//5, 5)`（每 5 金 +1，上限 +5），制造"存钱 vs 消费"决策。
- **定价**：武器 8 / 护符 10 / 物品 5 / 技能 6，附随机 ±1~2（防背公式），下限 3。
- **铁砧点（`meta.anvil_points`）**：**回退跨局 meta**（解锁武器/护符池用），**不进 build、不滚雪球、不与金币兑换**。防跨局买局内战力破坏 ante 曲线。
- 经济初值参考：起始 4 金 / 清房 +5 / Boss +10 / 利息每 5 金 +1 上限 +5（**需真机调**）。

---

## 12. 策略性设计（策略在哪、怎么防无脑）

### 12.1 六层策略（决策层 + 技能层）

| 层                 | 决策                                   | 取舍                                      |
| ----------------- | ------------------------------------ | --------------------------------------- |
| **build 构建**      | 武器槽 2–3 对 5 元素覆盖谁                    | 覆盖广(稀释带子/按停难/克制被摊薄) vs 覆盖窄(浓度高/好按停/怕不克) |
| **资源配比**          | 升级武器(线性,前期强) vs 买护符(乘法,后期复利)         | 投资重心何时切换                                |
| **概率工程**          | 编程带子分布：高base低权重(赌爆发) vs 低base高权重(求稳) | Boss 高方差 / 清怪低方差（老虎机特有，小丑牌没有）           |
| **进程时机**          | 精英房(高风险高回报)？攒利息 vs 立即战力？技能留 Boss？    | 风险与节奏                                   |
| **战斗内时机**         | 技能/消耗品何时用                            | 关键回合抉择（少量但爽）                            |
| **按停技能**（v2.1 新增） | 盯克制符号按停 vs 求快随便停                     | 高速下的眼力手速；带子越短越好按，与 build 层直接联动          |

> **build 层与技能层的耦合是本作特色**：带的武器越少，目标符号在带子上越密、越好按停。"带得少反而好打"这个反直觉的手感差异，是槽位设计能否立住的核心判据。

### 12.2 策略 → 爽感闭环

对的 build 决策（看敌人换元素覆盖 → 配对护符 → 保浓度 → 攒利息）→ 汇成一次爆炸数字。**决策是因，大数字是果**；"我算出来的"比数字乱涨更爽。

### 12.3 防无脑化四机制（已在框架内）

1. **四类初始配额 2/1/1/1 + 武器·技能无顶成长 · 护符硬顶生长 · 消耗品固定腰带**（进池类武器/技能 `UNCAPPED` 无天花板；护符 `CHARM_CAP=3` 买即开槽生长（1→3）；消耗品为固定 4 格腰带 `CONSUMABLE_CAP=4` 不随购买成长、整备仅选 1 类、余 3 格由商店重复购买同类填满）+ **各自独立槽位、互不算总** → 取舍来自每类内部（废铁/稀释自然刹车 + 金币递增价），不再有"总槽锁死他类"的误判。
2. **商店刷新随机** → 每局 build 不同，不能背公式。
3. **敌人元素随 ante 多样化** + **克制奖罚并存** → 倒逼按敌人弱点/抗性调整覆盖，不能一套打到底。
4. **按停技能层**（v2.1）→ 同样的 build 在不同玩家手里结果不同，消除"配好就自动赢"。

### 12.4 长线新鲜感的诚实说明

极简版策略**上限**较低。玩家长线新鲜感靠**内容广度**（更多武器/护符/敌人/房间/事件），而非再加系统。用内容撑长尾比用系统复杂度撑更健康、更好平衡。

---

## 13. 冻结清单（明确不做，及原因）

| 系统                                | 状态           | 原因                               |
| --------------------------------- | ------------ | -------------------------------- |
| **塔罗**（战斗内一次性改池）                  | ❌ 冻结         | 战斗内操作，与随机性冲突、心智负担重               |
| **星球**（独立类别养成）                    | ❌ 冻结         | 并入"武器升级"单线即可，不必独立系统              |
| **套装**（set_tag 数量门槛）              | ❌ 冻结         | 与护符槽冲突、增复杂度；深度靠 build 取舍已够       |
| **双货币兑换**                         | ❌ 冻结         | 铁砧点回退 meta，防跨局滚雪球                |
| **强惩罚 ×0.7**                      | ❌ 改为温和 ×0.85 | 奖罚并存但幅度温和，避免崩盘式挫败（§6.2）          |
| **逐符号复杂元素判定**                     | ❌ 简化         | 改"敌人弱点/抗性"呈现（§6.3）               |
| **属性抗性**（fire_def…）               | ❌ 冻结         | MVP 用单一护盾资源（§8），进阶再议             |
| **原 Phase F 扁平词缀**（AffixData 挂武器） | ❌ 废弃         | 与护符职能重叠，被本框架取代                   |
| **REELS 3→4**（v2.1 新增）            | ❌ 已重估·撤销     | 旧随机模型下的结论，与方案 A 的手动按停冲突；详见 §10.3 |
| **转轮自动停/兜底**（v2.1 新增）             | ❌ 明确不做       | 违反 P9；停止权完整归玩家，不停就一直转            |

> 冻结 ≠ 永不做；是"当前不做，避免一次堆太多"。真要加，逐个从清单解冻、单独评估。

---

## 14. 数据模型 / 代码映射

| 改动                 | 文件 / 位置                                                                                                              | 说明                                                                                                                           | 状态          |
| ------------------ | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 武器元素化              | `scripts/equipment/weapon_data.gd`                                                                                   | `@export var element = "none"`                                                                                               | ✅           |
| 符号继承元素             | `duel_controller.gd` `_build_pool` / `_effElement`                                                                   | `sym.element=="none"` 则继承武器 `element`；合并键 = `resource_path\|有效元素`                                                            | ✅           |
| 克制奖罚并存(温和)         | `scripts/battle/element_counter.gd`                                                                                  | `MULT_ADVANTAGE=1.5` / `MULT_RESIST=0.85` / 无关 1.0                                                                           | ✅           |
| 有效元素并行数组           | `duel_controller.gd` `grid_elem`                                                                                     | 与 `grid` 同形，记录每格的**有效元素**                                                                                                    | ✅           |
| 护符全局乘区             | `item_data.gd` `mult_value` + `_apply_charms` + `_agg_damage_mult`                                                   | `charm_damage_mult` 累乘，回合层结算                                                                                                 | ✅           |
| ante 曲线            | `duel_controller.gd` `_start_room`                                                                                   | 按幕台阶×幕内爬升：`ANTE_ACT_STEP_HP=1.75`/`ATK=1.46`、`ANTE_ROOM_STEP_HP=1.15`/`ATK=1.10`（数学等价原 `(1+α)^idx`，解耦两旋钮） | ✅           |
| 实体转轮带              | `duel_controller.gd` `_build_strips` / `_on_spin_tick` / `_lock_reel` / `_finish_spin`                               | 权重展开为格子 + 节拍器 + 手动停；`_STRIP_MIN_CELLS=30`                                                                                    | ✅           |
| 停止交互接线             | `battle_hud.gd` `_on_spin_button_pressed` / `_on_reel_clicked`                                                       | SPIN/空格 = 停下一列；点击列 = 锁该列                                                                                                     | ✅           |
| 槽位收紧（独立槽位）      | `duel_controller.gd` 常量区                                                                                              | `SLOT_INIT` 2/1/1/1；进池类（武器/技能）`UNCAPPED` 无天花板，不进池类 `CONSUMABLE_CAP=4`/`CHARM_CAP=3` 硬限（消耗品为固定 4 格腰带、不随购买成长）；四类统一「买即开槽」（仅武器/技能/护符）；**无全局总槽**（v2.1 的 `TOTAL_MAX=5` 共享闸门已移除） | ✅           |
| 武器升级(线性)           | 沿用 `meta.weapon_upgrades`                                                                                            | 按 `resource_path` 运行时加成，不改共享 .tres                                                                                           | ✅（已有）       |
| **伤害分解显示**         | `duel_controller.gd` `_evaluate` + `battle_hud.gd`                                                                   | 逐符号明细「基础×连线×克制=小计」+ 回合级乘区（护符/技能/强袭）汇总；呈现 = 敌人头顶飘字 + 编辑器控制台调试日志（中栏固定面板已移除，`d8ae32f`/`1b8c6e8`/`5264da5`）；兼作 ante 调参面板                                                           | ✅ 已落地（S2）   |
| **敌人弱点/抗性呈现**      | `battle_hud.gd` `_refresh_legend:967`                                                                                | 「敌人 X / 弱 Y ×1.5 / 抗 Z ×0.85」三行分色，走 `ElementCounter.weakness()/resists()/fmt_mult()` 反查                                      | ✅ 已落地（S3）   |
| **符号按元素着色**        | `battle_hud.gd` `_refresh_cell` + `_cell_element`                                                                    | 按 `grid_elem` 有效元素着色（S4 已落地：`_refresh_cell:1116` 用 `_cell_element` 取有效元素，none 时回落 `s.color`）                                 | ✅ 已落地       |
| **tooltip 元素 BUG** | `battle_hud.gd` `_on_cell_hover`                                                                                     | **已在 S1 修复**：`_on_cell_hover:889` 改读 `_cell_element(reel,row)` → `controller.grid_elem`（不再读符号原生 `s.element`），tooltip 与结算口径一致 | ✅ 已修复       |
| **防御统一为护盾**        | `duel_controller.gd` `charm_shield_trickle` + `_agg_shield()` + `_enemy_deal_damage`                                 | `受伤 = max(0, raw − shield)`；守备护符每回合护盾涓流，独立 armor 数值已删                                                                        | ✅ 已落地       |
| **局内金币/商店**        | `duel_controller.gd` `gold`/`_award_gold`/`_roll_shop`/`_on_shop_buy_pressed` + `battle_hud.gd` `_build_shop_screen` | 局内清零（起始4）；奖励后入商店随机刷新；利息每5金+1上限+5                                                                                             | ✅ S6–S8 已落地 |
| **S9 槽位成长（四类统一）** | `duel_controller.gd` `SLOT_INIT` + `_cat_max`/`_cat_cap`/`_can_grow_slot`/`_grow_slot`/`_cap_text` + `_shop_price` 按类步进递增 | **四类统一「买即开槽」**：初始 2/1/1/1；天花板 ∞/∞/4/3（进池类无顶，靠稀释+递增价；不进池类硬顶；消耗品为固定 4 格腰带、不再成长）；加价从「首次扩槽」起算（填初始空位仍原价）；四类互不算总 | ✅ 已落地 |
| **F-0 死代码清理**      | `duel_controller.gd`（删 `_affix_*` 5 个中性占位）+ 删 `equipment/enums.gd`、`stat_modifier.gd`、`weapon_data.gd` 死代码段 | 词缀已废弃，死代码已清（含纯死代码文件与 `WeaponData.get_implicit_modifiers/_mod`）                                                                          | ✅ 已落地       |
| ~~转轮 4 列~~         | ~~`REELS`~~                                                                                                          | **已撤销**，见 §10.3 / §13                                                                                                        | ⛔           |

---

## 15. 执行步骤（完成度台账 + 路线图）

> 本章为**权威进度记录**，每次落地后回写。状态：✅ 已落地 / 🔄 进行中 / ❌ 未开工 / ⛔ 已撤销。
>   
> 最近核对：**2026-08-03**（逐条对照代码复核，并修复一个潜伏严重的 run 收尾 BUG）。**追加批次 11–23**：
> - `620de1d` F-0 死代码清理（`_affix_*` 5 占位 + `enums.gd`/`stat_modifier.gd`/`weapon_data.gd` 死代码段）；
> - `afcfb29` 局内爆发杠杆①：special 三连「连锁重触发」（§10.4）；
> - `4afacf2` 跨局爆发杠杆：每局结束元进度三选一，膨胀双轨落地（§10.5）；
> - `333717a` **修复通关幕一 BOSS 即重开整局的严重 BUG** + 元进度改真·三选一 + 本文档 §10.4/§10.5 回写；
> - `161bad0` 「增益(buff)」重构为「技能(skill)」+ 物品来源标签 + 临时测试倍率（§7/分类职责）；
> - `cb7183b` 回写策划框架文档「增益→技能」+ 同步分类职责开放项；
> - `a75ea84` 拆分 `resources/items` 为 `charms/` + `consumables/`（代码侧 `ITEM_POOL` 显式合并两扫描路径）；
> - `489aee1` 更正「数值撞车」误判（狂怒护符 ×1.5 × 迅捷 ×1.5 = ×2.25 独立乘区真相乘，设计本意）。
> - `a37072c` 消耗品重构为固定 4 格腰带模型（允许同类重复占格、可商店重复购买；取代按类型 keyed 的 `consumable_charges`）。
> - `034cd06` 清理 `duel_controller.gd` 编辑器警告：`_shop_name()` 不可达死代码删除；`_roll_meta_choices` 局部 `pool`→`meta_pool` 消除遮蔽；`_on_shop_buy_pressed`/`_on_shop_sell_pressed` 价格/退款变量统一为函数级 `buy_price`/`sell_refund`（按用户指示保留 `ElementCounter`/`ResourceScan` 同名别名与整取整除法）。
> - `aec29b8` 修复上轮清理遗留的 3 处变量引用未改名编译报错（`_on_shop_buy_pressed` 非 active 分支两处 `_log` 漏改 `price`→`buy_price`、`_on_shop_sell_pressed` 非 active 分支 `_log` 漏改 `refund`→`sell_refund`；Godot 报 873/875/923 行 `Identifier not declared`，本次补齐）。
> - `388bbad` 拆分消耗品「整备上限」与「腰带容量」：`_cat_max('active')`/`_cat_cap('active')` 由返回 `CONSUMABLE_CAP`(4) 改回 `SLOT_INIT['active']`(1)（整备初始仅 1 格）；HUD 商店卡对 active 特判用 `CONSUMABLE_CAP=4` 显示腰带总容量；腰带购买上限恒 4（初始 1 + 商店补 3）。
> 见 §15.1 提交史第 11–23 行与 #15–#23。

### 15.1 已完成（15 项；S1–S10 已落地并推送，S11 基础量就位 / 扩展未开工）

> 提交历史（均已推送 `origin/master`）：
>
> | 批次 | 提交 | 内容 |
> | -- | -- | -- |
> | 1 | `dcaebb1` | Phase G MVP + 方案A + 槽位收紧 + S1–S4 呈现层 |
> | 2 | `3b2f6a6` | 右上角加成修复 + 克制层恒显 + 敌人 debuff 元素驱动 B |
> | 3 | `9926365` | S5–S8 经济与防御：统一护盾 / 金币商店利息 / 转轮金币符号 / 护符硬上限 / 消耗品可消耗 |
> | 4 | `cbcdd2a` | 台账纠正（S3 弱点呈现标已落地 / 补提交史 / 清经济层缺失旧表述） |
> | 5 | `dced572` → `bd34719` | **S9 武器槽成长（5 次反复）**：无硬顶实验 → `?:` 三元语法修 → 购买阻断修 → 武器攻击力差异化（专属符号 base 各异）→ 误退回硬上限 2 → 重做为「初始 2 + 金币扩槽」 |
> | 6 | `4dafcbc` + `f812fa3` | **移除全局 `TOTAL_MAX` 共享闸门**（四类互不算总）+ 修 `else` 缩进破损 |
> | 7 | `6ee8568` | 四类初始配额 2/1/1/1 + 统一「买即开槽」；整备屏改四列分区 + 槽位条 |
> | 8 | `bd88806` | **天花板改按「进池与否」分野**：武器/技能 `UNCAPPED` 无顶，消耗品/护符硬限 |
> | 9 | `46641a9` | 整备屏 UI 尺寸优化（卡片 110→66px / 列宽 236→250 / 规则文案精简） |
> | 10 | `89e0e74` | 否决「熔毁之间」独立房，改商店统一买卖（购入 + 卖出回收约50%金币）；`_shop_price` 回退按持有数计价并移除 `purchase_count` 反套利 hack；`paid_price`/`_sell_price`/`_on_shop_sell_pressed` + HUD「卖出」四列；BOSS 判定与房间语义排序保留 |
> | 11 | `620de1d` | **F-0 死代码清理**：删 `_affix_power/_shield/_regen/_damage_mult/_symbol_weight` 5 个中性占位（词缀轴已冻结，流程零调用）及其在 `_agg_*` 聚合层的调用；`git rm scripts/equipment/enums.gd`、`stat_modifier.gd`（纯死代码）；`weapon_data.gd` 删 `get_implicit_modifiers`/`_mod` 静态死函数（保留 `enum Archetype/WeaponType` 与 3 个 `@export` 被 8 把武器 .tres 引用）。文档台账同步至「已删除」（§15.6） |
> | 12 | `afcfb29` | **局内爆发杠杆①「连锁重触发」**（§10.4）：`CHAIN_STEP=1.5` / `CHAIN_MAX=4`；special 三连→免费重转，倍率只作用在 special 部分（峰值 ×3.375）；`_evaluate(chain_mult)` 新增 chain 形参、`_on_spin_pressed` 加连锁循环、敌人死亡即中断、伤害分解尾部显 `连锁×N` |
> | 13 | `4afacf2` | **跨局爆发杠杆：元进度三选一**（§10.5）：meta 字典扩 `weapon_base_bonus`/`charm_upgrades`；`_build_pool` 建 `_weapon_base_map` 于乘法前叠加（武器 base 线性 +3/级）；`_apply_charms` 的 `damage_mult` 分支按 `(1+0.12*级)` 增值、总乘区硬顶 ×6.0；锋锐护符加法→乘数 `×1.10`；三处 BOSS 通关结算点接 `_show_meta_choice()` |
> | 14 | `333717a` | **修复严重 BUG + 真·三选一**：新增 `_is_run_final(idx)`（idx≥ROOMS.size()-1 才算通关整局），三处奖励结算点改用之，修复「通关幕一 BOSS(idx=3) 即 `_full_reset()` 回到房 0 → 幕二/幕三与后两 BOSS 永久不可达」的 bug（自 S10 T3 起潜伏）；`_roll_meta_choices` 改为候选池 `shuffle()` 取 3（原返回全部候选、无取舍）；仅 `effect=="damage_mult"` 护符出升级卡（非乘区护符出卡=假选项）；`_start_room` 加越界兜底；本文档 §10.4/§10.5 回写 |
> | 15 | `161bad0` | **「增益(buff)」重构为「技能(skill)」+ 物品来源标签 + 临时测试倍率**：`buff_data.gd`→`skill_data.gd`（`resources/buffs/`→`resources/skills/`）；原 4 增益拆分（haste/rage 留技能、bulwark/regen 转护符，新增 `heal` 护符被动）；`battle_hud` 加 `_source_tag`（【武器/常驻/主动/转轮】）；`duel_controller` 加 `TEST_PLAYER_DMG_MULT=1.5`/`TEST_STATUS_DMG_MULT=3.0`（⚠ 正式平衡前须改回 1.0）；新增 `docs/[已完成]物品分类职责划分.md`；§15.9 #4 回写为「吸收进 F6」 |
> | 16 | `cb7183b` | **回写策划框架文档「增益→技能」+ 同步分类职责开放项**：`数值膨胀与策略深度设计框架.md` §7 槽位表「增益(buff)」→「技能(skill)」、相关正文引用同步；`[已完成]物品分类职责划分.md` 开放项/待办同步 |
> | 17 | `a75ea84` | **拆分 `resources/items` 为 `charms/` + `consumables/`**：11 个护符 .tres 与 4 个消耗品 .tres 经 git mv 归位；`duel_controller.gd` 的 `ITEM_POOL` 由单路径改为 `ResourceScan.scan_paths("res://resources/charms/") + ResourceScan.scan_paths("res://resources/consumables/")`（`scan_paths` 非递归须显式双路径）；两文档路径引用同步 |
> | 18 | `489aee1` | **更正「数值撞车」误判**：`[已完成]物品分类职责划分.md` §5 原建议「迅捷 `buff_value` 1.5 改 1.6 拉开」——经核对 `_agg_damage_mult() = charm_damage_mult × _buff_damage_mult()`，狂怒护符 ×1.5 与迅捷 ×1.5 为**独立乘区真相乘（= ×2.25）**，属设计本意而非撞车；撤销改 ×1.6 建议，保持 ×1.5 不动 |
> | 19 | `a37072c` | **消耗品重构为固定 4 格腰带模型（允许同类重复占格，可商店重复购买）**：新增 `consumable_slots` 腰带实例数组（每格 {path,item_id,charges,uid}），取代原按类型 keyed 的 `consumable_charges` + `selected_consumables` 混用；`CONSUMABLE_CAP` 2→4，消耗品不再「买即开槽」成长；商店购买同类写入独立腰带格（可四格全血瓶）、按腰带实占数递增价；卖出/使用按 `uid` 精准定位；战斗面板与 `_on_consumable_pressed` 改按 `uid`；用尽即移出该格；整备仅按 `selected_consumables` 去重勾选 **1 类（初始 1 格）** 确认时实例化为腰带（腰带总容量 `CONSUMABLE_CAP=4`，靠商店购买填充余下 3 格） |
> | 20 | `a37072c` | **消耗品固定 4 格腰带模型（允许同类重复占格）**：新增 `consumable_slots` 腰带实例数组（每格 {path,item_id,charges,uid}）取代按类型 keyed 的 `consumable_charges`；`CONSUMABLE_CAP` 2→4 且不再「买即开槽」成长；商店同类可重复购买（每购写入独立腰带格、可四格全血瓶）、按腰带实占数递增价；卖出/使用按 `uid` 精准定位、用尽移出该格；整备仅去重勾选 1 类（初始 1 格），确认时实例化为腰带（腰带总容量 `CONSUMABLE_CAP=4`，靠商店购买填充余下 3 格） |
> | 21 | `034cd06` | **清理 `duel_controller.gd` 编辑器警告**：删 `_shop_name()` 中 `return` 后的不可达死代码；`_roll_meta_choices` 局部 `pool`→`meta_pool` 消除遮蔽；`_on_shop_buy_pressed`/`_on_shop_sell_pressed` 价格/退款变量统一为函数级 `buy_price`/`sell_refund`；保留 `ElementCounter`/`ResourceScan` 同名常量别名、整取整除法、局部 `len`（按用户指示忽略） |
> | 22 | `aec29b8` | **修复编辑器警告清理遗留的 3 处引用未改名编译报错**：`_on_shop_buy_pressed` 非 active 分支两处 `_log` 仍用旧名 `price`→`buy_price`；`_on_shop_sell_pressed` 非 active 分支 `_log` 仍用旧名 `refund`→`sell_refund`（Godot 报 873/875/923 行 `Identifier not declared`） |
> | 23 | `388bbad` | **拆分消耗品「整备上限」与「腰带容量」**：`_cat_max('active')`/`_cat_cap('active')` 由返回 `CONSUMABLE_CAP`(4) 改回 `SLOT_INIT['active']`(1)（整备初始仅 1 格，此前误放宽到 4）；HUD 商店卡对 active 特判用 `CONSUMABLE_CAP=4` 显示腰带总容量（避免误判腰带 1 格即卖不动）；腰带购买上限恒 4（初始 1 + 商店补 3） |
> | 24 | `4d6b931` | **商店金币升级 tab（S12）：三页签商店 + 4 项局内金币升级，均折入现有 3 乘区不新增第 4 乘区**：`battle_hud` 商店重构为 购入/卖出/升级 三页签（自定义按钮行切换子面板 + 升级页卡片渲染）；`duel_controller` 新增 `gold_upgrades` 局内状态（每局 `_full_reset` 清零）、`GOLD_UPGRADE_DEFS`（锋锐研磨 power +基础/连线精通 line +连线倍率/护符共鸣 joker ×(1+N·0.25) 并入 buff_mult/壁垒 shield +开局护盾）、`_gold_upgrade_defs/_cost/_desc/_on_gold_upgrade_pressed`；效果经 `_agg_power_flat`/`_agg_damage_mult`/`_contribute`(连线倍率仅≥2生效、暴击按实落数)/`_start_room`(护盾)/伤害分解「共鸣」尾项 接入；价格随等级递增 |
> | 25 | `1978ff7` | **敌人护甲扁平池 + 反制即爆发 Plan C（§10.6）**：`RoomData.armor` / `SymbolData.pierce_armor` 数据层落地；9 间房配护甲、3 射击符号(`shot`/`pistol_shot`/`ice_shot`)设 `pierce_armor` 穿透；`duel_controller` 加 `_apply_enemy_damage`(先破甲后掉血) + `_on_counter`(`special` 三连通用破甲 / 克制元素三连追加 `enemy_hp_max×20%` 穿透核爆) + pierce 路由 + 伤害落地即时刷新；`rust_armor_gimmick` 熔铸护甲改为成长型护甲池(弃用 `boss_dmg_mult` 百分比减伤)；`battle_hud` 敌人面板「护甲 X/Y」标签(`Palette.ARMOR`，无护甲隐藏)；文档回写 §10.6/§8/§15.9 #2、BOSS 指南去 `boss_dmg_mult` 陈旧引用 |
> | 26 | `90822e3` | **P0 代码优化：抽取纯结算函数到 `battle_math.gd`（代码审查文档「阶段 P0」）**：新建 `scripts/battle/battle_math.gd`（`class_name BattleMath`，纯 `static func`，只进参数出返回值、不碰 hud/节点）；`duel_controller.gd` 的 15 个结算函数（`_agg_*`/`_buff_*`/`_status_*`）改为薄包装委托 `BattleMath.xxx(...)`（文档称「纯函数」实际读 controller 成员，故参数化传参而非盲移）；`_grant_buff`/`_tick_buffs` 因碰 hud 保留 controller；修复 `battle_math.gd:60` Godot 4.7 对 `min()` 复合 RHS 类型推断失败（显式 `float` 注解）；零行为变化、F6 数值一致后提交；路线图 P0 状态 🟢 见 `docs/[已完成]代码审查与优化建议.md` |
> | 27 | `5298c5b` | **P1 代码优化：引入 `BattleState` 只读快照，替换 HUD→controller 的 141 次直读（代码审查文档「阶段 P1」）**：新建 `scripts/battle/battle_state.gd`（`class_name BattleState`，RefCounted，54 个只读语义字段）；`duel_controller.gd` 新增 `state` 只读属性（`get = _build_state()` 每次返回最新快照）+ `_build_state()` 构建器；`battle_hud.gd` 117 处数据字段读（`grid`/`meta`/`enemy_*`/`charm_*`/`player_*`/`gold`/`room_index` 等）由 `controller.<FIELD>` 改为 `controller.state.<FIELD>`；逻辑/动作方法（`_cat_max`/`_cat_cap`/`_on_*_pressed`/`_roll_*`/`_shop_name` 等）保留 controller 调用（属 P2 信号范畴）；HUD 不再直读 controller 私有可变状态；路线图 P1 状态 🟢 见 `docs/[已完成]代码审查与优化建议.md` |
> | 28 | `3011c66` | **P1 fixup：还原 `consumable_panel` 节点引用**：`consumable_panel` 是 HUD 在 `_build_ui` 创建、存于 controller 的 UI 节点（非数据），P1 批量替换误将其路由为 `controller.state.consumable_panel`，因 `state` getter 每次返回全新 `BattleState` 且该字段为 Nil，导致 `_build_ui` 调用 `add_theme_constant_override` 时 F6 报 "Nonexistent function ... in base Nil"；`battle_state.gd` 移除 `var consumable_panel`、`_build_state()` 移除 `s.consumable_panel = consumable_panel`、`battle_hud.gd` 3 处还原为 `controller.consumable_panel`；快照只装数据不装节点 |
> | 29 | `0eabbe0` | **P1 fixup：动作/写回调用改直写 `controller`，不再经只读快照**：P1 批量替换误把「动作/写回」也路由到 `controller.state`，但 `state` 是只读快照 getter（每次访问返回全新 `BattleState`），对其赋值/重赋值不反映到 controller 真实字段 → F6 发现「胜利后奖励三选一卡片不生成」（`reward_choices` 为 `=` 重赋值，旧写法落入 throwaway 快照，`for rw in controller.state.reward_choices` 读到空快照）、`in_loadout` 守卫失效（`_show/_hide_loadout_screen` 标量写回快照）、`grid` 写快照违背只读语义（`_build_ui`）；`battle_hud.gd` 将 `_show_reward_screen`（`reward_is_boss`/`reward_choices`）、`_show/_hide_loadout_screen`（`in_loadout`）、`_build_ui`（`grid` 初始化与 TRASH_SYMBOL 占位）全部收敛到 `controller.`，`state` 仅作只读；grep 确认已无 `controller.state.<x>=/append` 等写操作 |
> | 30 | `8c922bd` | **P2 代码优化：HUD 意图信号 + 自构建 + controller 公开接口（代码审查文档「阶段 P2」）**：`battle_hud.gd` 顶部声明 7 个意图信号（`spin_requested`/`reel_clicked`/`buy_requested`/`sell_requested`/`reward_chosen`/`boss_reward_chosen`/`reward_skip_requested`）；新增公开语义接口 `build_all()`（内部调各 `_build_*` + `_show_loadout_screen`，HUD 自构建全部界面，controller 不再调 `hud._build_ui` 等私有构建方法）、`set_reel_enabled(reel, enabled)`（替代 controller 直戳 `hud.cells[r][0].disabled`）、`hide_reward_screen()`/`hide_meta_screen()`/`hide_shop_screen()`/`hide_anvil_screen()`（替代直戳 `hud.*.visible=false`）；点击交互/商店买卖/奖励三选一 8 处 `controller._on_*` 直连改为 `signal.emit.bind(arg)` 按值绑定（规避循环变量闭包坑，rw.id/cand/offer/slot["uid"]/path 均为函数参数或即时按值捕获）；`duel_controller.gd` `_ready` 订阅 7 信号到对应 `_on_*` 处理器，并删 3 处冗余 `if hud.cells.size()...` 守卫；单向数据流 + 信号解耦落地。范围外（purify/reload/loadout/meta/anvil/gold_upgrade/overlay 按钮）的 `controller._on_*` 直连保留，属后续解耦 |
> | 31 | `cfce4e5` | **P3a 代码优化：平衡常量 @export 化（代码审查文档「阶段 P3」前半）**：将 `duel_controller.gd` 中 24 个「策划可调平衡常量」由 `const` 改为 `@export var`（附带显式类型，移交 Duel 节点 Inspector 拖拽调参）：难度曲线 `ANTE_ACT_STEP_HP/ATK`、`ANTE_ROOM_STEP_HP/ATK`；命中/废铁 `MISS_FLOOR`、`MISS_CEIL`、`KPI_EXPLOSION_PER_ROOM`；伤害基准/爆发 `BASE_POWER_REF`、`CHAIN_MAX`、`CHAIN_STEP`（⚠ `SPECIAL_TRIPLE_CRIT` 已于「方案 A」退役，改为 `LoadoutItem.crit_mult`，普通/special 三连统一暴击）；元进度成长 `WEAPON_BASE_STEP`、`WEAPON_HIT_STEP`、`CHARM_MULT_STEP`、`CHARM_MULT_CAP`、`META_ANVIL_BONUS`、`META_CHOICE_COUNT`；局内金币升级(S12) `POWER_STEP`、`LINE_STEP`、`JOKER_STEP`、`JOKER_CAP_FACTOR`、`SHIELD_STEP`；经济 `GOLD_POOL_WEIGHT`、`GOLD_PER_COIN`。默认取值与重构前完全一致，运行时行为不变。保留 `const`（结构性/测试旗标）：`REELS`/`ROWS`/`SLOT_INIT`/`UNCAPPED`/`CONSUMABLE_CAP`/`CHARM_CAP`/`ROOM_KIND_RANK`、`_SPIN_*`、`_STRIP_MIN_CELLS`、`_ITEM_STRIP_TARGET`、`_GOLD_CELLS`、`GOLD_UPGRADE_DEFS`、`STATUS_NAMES`、`TEST_PLAYER_DMG_MULT`、`TEST_STATUS_DMG_MULT`。P3b（.tscn 编辑器重建 battle_hud + 5 套覆盖层、脚本改 % 引用）留作后续阶段 |
> | 32 | `5ab165f` | **P3b-1 代码优化：主 HUD 静态骨架迁 `battle_hud.tscn`（代码审查文档「阶段 P3」后半，分两步走之第一步）**：`battle_hud.tscn` 由空壳（仅 `BattleHud` Control+script）重写为完整主 HUD 静态骨架——`Bg`(ColorRect)→`Margin`→`Content`(VBox)→标题/信息栏(RoomLabel/TurnLabel/RunLabel)/LoadoutLabel/`MainRow`（左玩家面板 `PlayerPanel`[instance ui_panel] + 中栏 `CenterCol`[ReelCenter→GridContainer、IntentBox、DmgBreakdownBox、LegendBox] + 右敌人面板 `EnemyPanel`[instance ui_panel]含 LogScroll→LogLabel）/底部操作栏 `BottomBar`（Spin/Purify/Reload/Reset[instance ui_button]+ConsumablePanel）；烘焙调色板/字号 + 2 个 StyleBoxFlat 子资源(sb_dmg/sb_legend)；29 个被引用节点标 `unique_name = true`。`battle_hud.gd` 主 HUD 23 个静态节点变量改 `@onready var ... = %Name`；`_build_ui` 删静态创建（约 230→50 行），仅留动态内容（转轮格子 SYMBOL_CELL 实例化循环 + 底部按钮信号/快捷键连接 %SpinButton/%PurifyButton/%ReloadButton/%ResetButton + `controller.consumable_panel = %ConsumablePanel` + `_build_overlay/_build_symbol_tooltip/_build_popup_layer`）；load_steps 修正 60→6。5 套覆盖层（整备/奖励/元进度/商店/铁砧）仍代码构建，留待 P3b-2 抽独立 `.tscn`；`_make_side_panel` 现无调用方（P3b-2 覆盖层面板或复用）。⚠ 尚未 F6，主界面显隐/对齐/转轮格子待验证 |
> | 33 | `2aab649` | **P3b-1 修复：`battle_hud.tscn` 手写 parent 路径修正 + 脚本改 `$节点路径`**：F6 报 `Node not found: "%GridContainer"`。根因——`5ab165f` 手写 `.tscn` 的 `parent` 误用直接父节点名（如 `parent="Content"`），而 Godot `.tscn` 要求 `parent` 为从场景根出发的完整路径（如 `parent="Margin/Content"`），致二级以下节点父引用失效被丢弃。修复：`.tscn` 全部 `parent` 改根起完整路径；脚本 `@onready`/`_build_ui` 节点引用由 `%Name` 唯一名改为确定性 `$节点路径`（绕开手写 `.tscn` 含 instance= 覆盖时唯一名注册的不可靠环节）。静态校验：父路径完整性 OK、29 条 `$路径` 全解析到真实节点、括号配平 OK。⚠ 待 F6 复验主界面 |
> | 34 | `7ff9382` | **P3b-2 代码优化：5 套覆盖层抽独立场景（loadout/meta/reward/anvil/shop）**：新增 `scenes/ui/{loadout,meta,reward,anvil,shop}_screen.tscn` 静态骨架 + `scripts/ui/{...}_screen.gd`（每屏自包含：显隐/填数据/切页签/发信号；`class_name` 全局类 + `configure(controller, hud)` 注入，卡片构造复用 HUD 的 `_make_*_card`/`_label`/`_item_pool_of`/`_source_tag`）。`battle_hud.gd` 瘦身 607 行：5 个 `_build_X_screen` 改 `instantiate → add_child → configure → hide` 委托；**统一修复 configure() 先于 add_child 导致 @onready 为 null 的崩溃隐患**（@onready 在 `_ready()`（入树时）才初始化，故必须先 add_child）；loadout 四列构建/刷新/计数/选中同步全部迁入 `loadout_screen.gd`，HUD 仅留 5 个薄包装（`_show/_hide_loadout_screen`、`_update_loadout_cards_visual/_count/_anvil`）保持 duel_controller 接口不变（controller 零改动）。共享构造器 `_make_item_card`/`_item_pool_of`/`_source_tag` 保留 HUD。静态校验：5 套 tscn 父路径完整性 OK、全部 @onready `$路径` 可解析、括号配平 OK、跨文件 hud.*/controller.* 引用符号全存在。⚠ 尚未 F6，须逐屏复验 5 覆盖层 + 主界面 |
> | 35 | `3f8aefb` | **P3b-2 修复：覆盖层脚本 `hide()` 改名 `hide_screen()`**：F6 报 `The method "hide()" overrides a method from native class "CanvasItem". This won't be called by the engine and may not work as expected. (Warning treated as error.)`——Godot 4.4+ 将脚本方法与原生类同名（`CanvasItem.hide`）列为 `native_method_override` 警告，本项目按 error 处理 → 编译失败。修复：5 套覆盖层 `func hide()` → `func hide_screen()`（与 `show_screen()` 对称），`battle_hud.gd` 8 处调用同步改名（`_build_*_screen` 收尾隐藏 + `hide_*_screen()` 公开接口 + `_hide_loadout_screen` 薄包装）。静态校验：无残留 `.hide()`、5 套 tscn 校验全绿、括号配平 OK。⚠ 待 F6 复验 |
>
>
> | 36 | `1e4772c` | **feat(Option C)：商店改 opt-in + 新增房间歇态 INTERROOM**：落地设计文档 `docs/[已完成]UI重做与流程重构执行方案.md` 的 Option C 最小验证。替代「每房强制全屏商店」——房奖励结算（普通/精英/BOSS 三处）改调 `_enter_interroom()`；新增 `in_interroom` 布尔 + `_enter_interroom`/`_on_shop_requested`/`_on_next_room_pressed`；`_on_shop_leave_pressed` 拆开（离开商店回间歇态，不自动进房）；SPIN/purify/consumable 三闸门追加 `or in_interroom`；HUD 顶栏 InfoBar 末尾加 🛒商店/▶下一房 两按钮（默认禁用，`set_interroom_enabled` 切换）；`_ready` 订阅新信号，`_full_reset` 复位 `in_interroom`。间歇由「必须点 ▶ 下一房才前进」结构性保留（设计 D3）。⚠ 待 F6 复验手感 |
> | 37 | `061b974` | **refactor(P3c)：转轮沉底常驻 dock + 中央动画舞台 + 移除游戏内整备入口**：`battle_hud.tscn` 重写布局——转轮 GridContainer 从 CenterCol 搬到屏幕底部 ReelDock 常驻（间歇/战斗均可见）；中央新增 CenterStage(VBox)=StageRow(玩家/敌人立绘占位 ColorRect)+BottomRow(伤害分解+图例)；战斗日志从 EnemyPanel 移到独立 LogBar；敌人意图/状态从 CenterCol/IntentBox 移入 EnemyPanel/VBox。`battle_hud.gd` 同步更新全部 @onready + 内联 `$路径`、移除 reload_btn（@onready/shortcut/connect/_chain_focus 四处）。`duel_controller.gd` 删除 `_on_reload_loadout_pressed`（游戏内重新配装入口），开局预跑整备仍由 `build_all()->_show_loadout_screen` 触发不受影响。5 套 modal 覆盖层零改动。`verify_overlays.py` 扩展支持 battle_hud.tscn（兼容 instance= 节点与实例内部路径）。⚠ 待用户 F6 复验布局 |
> | 38 | `06d7832` | **feat(P4)：战斗动画骨架 + 敌人立绘替换**：新增 `scripts/ui/battle_animator.gd`（`class_name BattleAnimator`，tween 驱动：玩家/敌人攻击前冲 scale 脉冲+轻微旋转、受击红闪 modulate、暴击/核爆居中大字 Label 弹入+屏震[viewport canvas_transform 偏移]）；结算点挂接——`_evaluate` 总伤害→`play_attack(player,enemy)`、`_on_counter`(special/element 三连破甲/核爆)→`play_counter`、`_enemy_deal_damage`→`play_enemy_attack`；只读演出、绝不修改 BattleMath 结算（解耦原则）。`EnemyPlaceholder`(ColorRect)→`EnemySprite`(TextureRect) 引用 `assets/enemy.png`（与玩家对称）；节点重命名 `PlayerPlaceholder`→`PlayerSprite`/`EnemyPlaceholder`→`EnemySprite` 供 P4 引用；`battle_hud._build_ui` 末尾创建 animator 并 `setup(self)` 缓存两 sprite。击杀溶解 `play_kill` 方法已备、接入点待定（won overlay 时序）。⚠ 待 F6 复验动画手感 |
> | 39 | `d8ae32f` | **refactor(P4 微调)：伤害反馈改头顶飘字 + 转轮/SPIN 整组居中**：移除 BottomRow `DmgBreakdownBox`（固定面板），伤害分解改为敌人头顶多行飘字（`battle_hud._float_breakdown`，复用 popup_layer，`_show_damage_breakdown` 改为调用它、`_clear_damage_breakdown` 隐藏复用 Label）；新增 `player_sprite`/`enemy_sprite` @onready 与 `_player_sprite_anchor`/`_enemy_sprite_anchor`，`duel_controller` 全部伤害/治疗/护盾/金币/状态/连锁/核爆 `_popup` 锚点由侧栏面板改为角色 Sprite（飘在头顶）；`ReelDock` 加 `size_flags_horizontal=SHRINK_CENTER` 整组居中、移除 `BotSpacer` 右推撑宽。⚠ 待 F6 复验 |
> | 40 | `2909595` | **fix(P4)：修复屏震 tween 类型错误**：`_apply_shake(base, off)` 误用 `Callable.bind(base)` 把 Transform2D 基准绑到参数末尾，使 tweener 把 Vector2 偏移当作第一个 Transform2D 参数传入而报错 `Cannot convert argument 1 from Vector2 to Transform2D`。改为新增实例变量 `_shake_base` 存基准变换，`_apply_shake(off: Vector2)` 仅接收偏移，消除类型不匹配。⚠ 待 F6 复验屏震 |
> | 41 | `5620afd` | **feat(D2)：商店右侧抽屉**：`shop_screen.tscn` 根节点由全屏覆盖层改为右侧 docked——`anchor_right=1.0` + 收起态 `offset_left=0`（零宽遮右缘），`show_screen` tween `offset_left` 0→-420 滑入、`hide_screen` 反向滑出后 `visible=false`；新增左侧金色描边 `Edge`(ColorRect, 4px)；逻辑（configure/refresh/show_tab）零改动。`shop_screen.is_shown()` + `hud.shop_screen_is_open()` 供 toggle；🛒 按钮在「🛒 商店 / 🛒 收起」间切换（`_on_shop_requested` toggle + `set_shop_button_text`）；离开按钮改「关闭 ✕」（`_on_shop_leave_pressed` 复位文案）；`_on_next_room_pressed` 进下一房前若抽屉展开先收起+复位文案。`load_steps` 仍 2（Edge 为内置 ColorRect 无新 ext_resource）。⚠ 待 F6 复验抽屉手感 |
> | 42 | `e93b85e` | **refactor(奖励流程)：房奖励改经典直接弹出**：移除胜利与奖励弹层之间的「胜利浮层·领取奖励 ▶」中间闸门——3 处胜利分支（普通击杀、状态结算击杀、重转击杀）的 `_show_overlay(title,"领取奖励 ▶")` 改为直接 `hud._show_reward_screen(_is_boss_room(room_index))`（原 `is_boss`/`title` 两临时变量随之移除）；`_on_overlay_button_pressed` 的 `won` 死分支清理，仅保留 `lost`(重试)/`cleared`(整局重置) 浮层。`reward_screen` 本就是全屏 modal，基础设施复用。附带收益：彻底消除旧 bug（15fa555）的浮层残留重弹根源。⚠ 待 F6 复验 |
> | 43 | `6336be4` | **refactor(奖励弹层居中)：reward_screen 由铺满全屏改为居中卡片**：根 `RewardScreen` 保留全屏 Control 作输入闸门（`mouse_filter=IGNORE`），内部新增 `Dimmer`(ColorRect 半透明 `Palette.BG_OVERLAY` alpha0.72, STOP 捕获点击) + `CenterContainer`(满屏居中) 承载 `PanelContainer`(Dialog, `custom_minimum_size=680x460` + 金色描边圆角 `sb_dlg` 子资源)；内容 `Margin/Content` 层级下移一层。`reward_screen.gd`：`bg`→`dimmer` 引用、`$Bg`→`$Dimmer`、4 处内路径加 `Center/Dialog`、颜色改 `Palette.BG_OVERLAY`。游戏在暗化层后隐约可见，弹层面积大幅缩小居中。⚠ 待 F6 复验 |
> | 44 | `20977e8` | **refactor(净化→消耗品)：删常驻净化按钮 + 消耗品腰带改 4 格子 2x2**（初版放 ReelDock/TopRow/ConsumablePanel，转轮左侧）：常驻净化按钮（`ReelDock/PurifyButton`+`PurifyLabel`+`Ctrl+P` 快捷键）与 `purify_charges`/`purify_max_base`/`charm_purify_bonus` 整条线全部删除——净化完全走消耗品「净化药剂」（charges 用完即移出腰带）。`battle_hud.tscn` 删 PurifyButton/PurifyLabel/BotSpacer；`ConsumablePanel` 由 HBoxContainer 改 GridContainer `columns=2`，内嵌 4 个静态 `Cell1-4`（参考右栏参考图虚线格子，`ui_button.tscn` 实例化，`custom_minimum_size=86x52`，`unique_name=false` 用确定性 $路径）。`battle_hud.gd`：4 cell 改 `@onready var consumable_cells: Array`，新增 `_refresh_consumable_panel()`（按 `consumable_slots` 顺序填、空位 "—" 禁用）与 `_on_consumable_cell_pressed(idx)` 转发 `controller._on_consumable_pressed(uid)`；`_build_ui` 删 `purify_btn` 信号/快捷键、`_chain_focus` 同步；`_refresh_meta` 删 `purify_label` 行 + "净化上限" 概览；敌人意图 `（可净化）` → `（用净化药剂）`、删 "已净化(无)" 状态。`duel_controller.gd`：删 `_on_purify_pressed`；`_apply_charms` 删 `pm`/`charm_purify_bonus`/`purify_max_base` 整条净化计算；`_apply_reward` 删 `purify` 分支；`_start_room` 删 `purify_charges = purify_max_base`；`_on_consumable_pressed` 的 `purify` 分支去掉 `purify_charges += data.value`（data.value 字段废弃）；`_rebuild_consumable_panel` 整个函数删除（HUD 自管 4 cell 永不增删，4 处旧调用改 `_refresh_consumable_panel`）。`battle_state.gd` 删 `purify_max_base`/`purify_charges` 字段。**资源删除**：`resources/charms/purify_charm.tres`（丰沛护符·净化上限 +1）+ `resources/rewards/purify.tres`（净化符文·净化上限 +1）`git rm`；`resources/consumables/purify_potion.tres` description 改"清除敌人的干扰意图"、value 0（不再用）。**入口统一**：净化药剂从 4 格子腰带主动点击使用，`duel_controller._on_consumable_pressed(uid)` 走统一扣 charges+结算路径（其他消耗品同款）。**docs 同步**：`[已完成]物品分类职责划分.md` 删丰沛护符行 + 净化药剂效果简化 + "净化" 判定行改"净化仅走消耗品路径"；`装备收集规划_200.md` 护符数 11→10、`purify_bonus`/`purify` 行加注；`[已完成]UI_最佳实践改造方案.md` 删 Ctrl+P 净化快捷键；`BOSS机制扩展指南.md` 呓语锁轮设计意图改"逼玩家为净化药剂留位"；`whisper_lock_gimmick.gd` 注释同步。静态校验：`verify_overlays.py` 全绿（49 节点父路径 OK、23 @onready + 30 $Margin 全解析、括号配平 OK）。⚠ F6 已验：启动报 `game_state` 错（已在 `62e7196` 修），4 格子位置后被迁到左栏（见 #45） |
> | 45 | 待提交 | **refactor(ui)：消耗品 4 格子从转轮左侧迁到玩家左栏**：按用户反馈"放到左侧栏位里面去"——`ConsumablePanel`+`Cell1-4` 从 `ReelDock/TopRow/ConsumablePanel/` 移到 `PlayerPanel/VBox/ConsumablePanel/`，新增小标签 `ConsumableTitle`（"消耗品"，紫字 12pt）置于 GearBox 之后。格子尺寸 86×52 缩到 58×44 以适配 `PlayerPanel custom_minimum_size=150`（两列 58+4+58=120 留余量），`font_size` 13→12。`battle_hud.gd` `@onready var consumable_cells` 4 条路径同步（`CenterStage/ReelDock/TopRow/ConsumablePanel` → `PlayerPanel/VBox/ConsumablePanel`）；`_refresh_consumable_panel` 函数无逻辑改动。`ReelDock/TopRow` 仅剩 `ReelCenter`（结构未动）。静态校验：`verify_overlays.py` ALL CHECKS PASSED（路径全部解析、括号配平）。⚠ 待 F6 复验左栏格子显示+点击 |
>
> ⚠ **全部批次均未经真机 F6 验证**（本机无 Godot 二进制，仅静态校验：括号配平 + 缩进块检查）；`.bak` 已删，回退只能 `git revert`。

| #  | 项                              | 落地内容                                                                                                                                                                                                                                                                                      | 归属    |
| -- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| 1  | **武器元素化 + 符号继承 + 攻击力差异化** | `WeaponData.element`；`_effElement` 继承；合并键含有效元素；**每把武器专属伤害符号 base 各异**（axe 14 / iron 11 / fire 10 / ice 9 / pistol 8 / dagger 7 / staff special 25），解决共用 `slash.tres` 固定 base=10 的"攻击力无差异"问题 | §6.1  |
| 2  | **克制奖罚并存（温和）**                 | `element_counter.gd` ×1.5 / ×0.85 / ×1.0                                                                                                                                                                                                                                                  | §6.2  |
| 3  | **护符全局乘区**                     | `ItemData.mult_value` + `charm_damage_mult` → `_agg_damage_mult`；示例 `rage_charm.tres`（×1.5）                                                                                                                                                                                               | §9    |
| 4  | **ante 难度曲线**                  | 按幕台阶×幕内爬升：`ANTE_ACT_STEP_HP 1.75`/`ATK 1.46`、`ANTE_ROOM_STEP_HP 1.15`/`ATK 1.10`（数学等价原 `(1+α)^idx`）                                                                                                                                                                                                                                    | §10.2 |
| 5  | **实体转轮带 + 手动停**（文档外新增，v2.1 回写） | `reel_strips` / `_spin_timer` / 三条停止入口 / 无自动兜底 / `_STRIP_MIN_CELLS=30`                                                                                                                                                                                                                    | §4    |
| 6  | **整备槽位模型定稿**（文档外新增，v2.1 回写）      | 四类**各自独立、互不算总**（v2.1 的全局 `TOTAL_MAX=5` 共享闸门已移除——它会导致「带满护符即锁死武器」误判）；初始配额 `SLOT_INIT` = 武器2·技能1·消耗品1·护符1                                                                                                                                                              | §7    |
| 7  | **S1 修 tooltip 元素 BUG**        | 新增 `battle_hud._cell_element()` 统一读 `grid_elem`（有效元素）；tooltip 附带对敌倍率 `×1.5`                                                                                                                                                                                                               | §6.1  |
| 8  | **S2 伤害分解面板**                  | `_contribute` 逐符号推 `acc["lines"]`；`_evaluate` 汇总回合级乘区；中栏新增 `dmg_breakdown_box`                                                                                                                                                                                                            | §5.2  |
| 9  | **S3 敌人弱点/抗性呈现**               | `ElementCounter.weakness()/resists()/fmt_mult()` 反查；图例首行改「敌人 X / 弱 Y ×1.5 / 抗 Z ×0.85」三行分色                                                                                                                                                                                                | §6.3  |
| 10 | **S4 符号按元素着色**                 | `_refresh_cell` 按有效元素给边框/底色/字色；`_cell_style_cache` 按元素缓存 StyleBox                                                                                                                                                                                                                         | §6.1  |
| 11 | **敌人 debuff 元素驱动**（回退 A·落地 B）  | 武器带属性→其元素 status 符号（火→燃/冰→霜/毒→毒）天然入该武器 reel，权重提升（burn/frost 7、poison 8）；燃=DoT、霜=减攻、毒=减攻+DoT。`_build_pool` 不再运行时归一化；`_enemy_deal_damage` 为唯一闸口统一乘算减攻。非元素武器（匕首/铁剑/战斧/手枪）按设计不带 debuff（暴力流 vs 控制流 trade-off）                                                                                  | §6.4  |
| 12 | **S5 防御统一为护盾**                 | 删独立护甲数值（`player_armor/charm_armor/_agg_armor/_affix_armor` 全删）；`guard_charm.tres` 改为 `effect="shield"`、每回合 +4 护盾（涓流，走 `_agg_shield()`，与铁壁护符同通道）；`_apply_charms` 增 `charm_shield_trickle` 分支；`_enemy_deal_damage` 简化为 `受伤 = max(0, raw − shield)`；HUD 删「护甲」标签。防御收成单一护盾资源（开局爆发 + 每回合涓流两种节奏） | §8    |
| 13 | **S9 槽位成长（四类统一）+ 整备屏四列改造** | `SLOT_INIT` 初始配额 2/1/1/1；天花板按「进池与否」分野（武器/技能 `UNCAPPED` 无顶，消耗品 2 / 护符 3 硬限）；`_cat_max`/`_cat_cap`/`_grow_slot`/`_can_grow_slot`/`_cap_text` 五个入口；`_shop_price` 按类步进且加价起点对齐初始配额。HUD 三列→四列（武器/技能/消耗品/护符），每列一条槽位条 ◆◇·＋ | §7    |
| 14 | **S12 商店金币升级 tab（深化已有乘区）** | 三页签商店（购入/卖出/升级）；4 项金币升级均折入现有 3 乘区（连线/护符·增益/克制）、不新开第 4 乘区：锋锐研磨 +基础伤害、连线精通 +连线倍率、护符共鸣 ×(1+N·0.25) 并入 buff_mult、壁垒 +每房开局护盾；`gold_upgrades` 局内状态每局清零；价格随级递增 | §10.1/§11 |
| 14 | **统一商店（购入+卖出）取代「熔毁之间」+ run 结构地基（S10 部分落地）** | `RoomData` 新增 `kind`(normal/elite/boss)；`_sort_rooms` 按 kind 语义重排（修旧字母序难度倒挂）；BOSS 判定改 `_is_boss_room(idx)`；`_shop_price` 恢复**按持有数**计价（`max(0, owned-SLOT_INIT+1)*step`），售出使持有数回落、换装成本=买卖价差（防刷价）；新增 `paid_price`(path→实际购入价) + `_sell_price`(返还约50%) + `_on_shop_sell_pressed`（武器保底 `LOADOUT_MIN` 不可卖，其余可全清释放槽位/稀释转轮/回收金币）；HUD 商店同屏新增「卖出」四列。**否决**独立「熔毁之间」房（其 `_refit_*` 销毁/替换逻辑已整体删除、`scrap_shrine.tres` 已移除）——统一进商店 UI 更少，护符「替换」别扭问题自然消解。**已提交 `89e0e74`** | §7/§10/S10 |
| 15 | **F-0 死代码清理 + 双爆发杠杆（局内连锁重触发 + 跨局元进度三选一）+ run 收尾严重 BUG 修复** | 见提交 `620de1d`/`afcfb29`/`4afacf2`/`333717a` 与 §10.4 / §10.5 正文；核心：① special 三连免费重转、倍率仅作用 special（峰值 ×3.375）；② 每局结束三选一（武器 base 线性 +3/级 × 护符乘区 +0.12/级、总乘区硬顶 ×6）；③ 锋锐护符加法→乘数；④ 修「通关幕一 BOSS 即重开整局」导致幕二/三不可达的潜伏 BUG（新增 `_is_run_final`） | §10 |
| 16 | **「增益」重构为「技能」+ 物品三分类来源标签** | `buff_data.gd`→`skill_data.gd`（机制照搬：进轮/初期限1）；原 4 增益拆分——`haste`/`rage` 留技能、`bulwark`/`regen` 转护符（新增 `heal` 护符被动，每回合 +6 回血，对称接入 `_agg_regen`）；`battle_hud` 加 `_source_tag` 来源标签（【武器/常驻/主动/转轮】）覆盖整备/商店/卖出/玩家 buff 行；`duel_controller` 加临时测试倍率 `TEST_PLAYER_DMG_MULT=1.5`/`TEST_STATUS_DMG_MULT=3.0`（⚠ 平衡前须改回 1.0）；新增 `docs/[已完成]物品分类职责划分.md` | §7/分类职责 |
| 17 | **策划框架文档回写「增益→技能」+ 分类职责开放项同步** | `数值膨胀与策略深度设计框架.md` §7 槽位表「增益(buff)」→「技能(skill)」、相关正文引用同步；`[已完成]物品分类职责划分.md` 开放项/待办同步（提交 `cb7183b`） | §7/分类职责 |
| 18 | **`resources/items` 拆分为 `charms/` + `consumables/`** | 11 个护符 .tres + 4 个消耗品 .tres 经 git mv 归位；`duel_controller.gd` 的 `ITEM_POOL` 改显式合并两 `ResourceScan.scan_paths` 路径（`scan_paths` 非递归，拆目录后须双路径）；两文档路径引用同步（提交 `a75ea84`） | §7/分类职责 |
| 19 | **更正「数值撞车」误判** | 经核对 `_agg_damage_mult() = charm_damage_mult × _buff_damage_mult()`，狂怒护符 ×1.5 与迅捷 ×1.5 为独立乘区真相乘（= ×2.25），属设计本意；撤销「迅捷改 ×1.6 拉开」建议，保持 ×1.5 不动（提交 `489aee1`） | §5/分类职责 |
| 20 | **消耗品固定 4 格腰带模型（允许同类重复占格）** | 新增 `consumable_slots` 腰带实例数组（每格 {path,item_id,charges,uid}）取代按类型 keyed 的 `consumable_charges`；`CONSUMABLE_CAP` 2→4 且不再「买即开槽」成长；商店同类可重复购买（每购写入独立腰带格、可四格全血瓶）、按腰带实占数递增价；卖出/使用按 `uid` 精准定位、用尽移出该格；整备仅去重勾选 1 类（初始 1 格），确认时实例化为腰带（腰带总容量 `CONSUMABLE_CAP=4`，靠商店购买填充余下 3 格）（提交 `a37072c`） | §7/分类职责 |
| 21 | **清理 `duel_controller.gd` 编辑器警告** | 删 `_shop_name()` 中 `return` 后的不可达死代码（UNREACHABLE_CODE）；`_roll_meta_choices` 局部 `pool`→`meta_pool` 消除对成员符号池 `pool` 的遮蔽（SHADOWED_VARIABLE）；`_on_shop_buy_pressed` 两条价格路径统一为函数级 `buy_price`、`_on_shop_sell_pressed` 两条退款路径统一为函数级 `sell_refund`（CONFUSABLE_LOCAL_DECLARATION）；保留 `ElementCounter`/`ResourceScan` 同名常量别名（项目静态调用约定）、整取整除法、局部 `len`（按用户指示忽略）（提交 `034cd06`） | §7/分类职责 |
| 22 | **修复警告清理遗留的 3 处变量引用编译报错** | `_on_shop_buy_pressed` 非 active 分支两处 `_log` 仍用旧名 `price`→`buy_price`；`_on_shop_sell_pressed` 非 active 分支 `_log` 仍用旧名 `refund`→`sell_refund`（Godot 报 873/875/923 行 `Identifier not declared`）；提交 `aec29b8` | §7/分类职责 |
| 23 | **拆分消耗品「整备上限」与「腰带容量」** | `_cat_max('active')`/`_cat_cap('active')` 由返回 `CONSUMABLE_CAP`(4) 改回 `SLOT_INIT['active']`(1)，整备初始仅 1 格；HUD 商店卡对 active 特判用 `CONSUMABLE_CAP=4` 显示腰带总容量；腰带购买上限恒 4（初始 1 + 商店补 3）；修正此前把整备勾选上限误放宽到 4 的理解偏差（提交 `388bbad`） | §7/分类职责 |

**顺带修掉的坑**：`_free_spin` 补 `_busy=false`（重转卷轴后软锁）；`_apply_chaos_pool` 补 `p[2]`（chaos 后索引越界）；`battle_hud._refresh_legend` 内层 `var sig` 重名（GDScript 函数级作用域）。移除死代码 `_do_spin_core` / `_weighted_random_sym`。

**S4 附带修正**：格子在非旋转期是 `disabled` 态，原先只覆盖 `font_color` 与 `normal` stylebox → 禁用态下**从未生效**（走的是主题默认样式）。现四态（normal/hover/pressed/disabled）字色与 StyleBox 全部覆盖。

### 15.2 ✅ MVP 呈现层补齐（S1–S4，已落地）

> 根因同一个：MVP 只改了控制器，HUD 没跟上（`grid_elem` 在 HUD 侧曾零引用）。现已由 `_cell_element()` 统一接入。

| 步骤     | 任务                                       | 验收                                                               | 状态 |
| ------ | ---------------------------------------- | ---------------------------------------------------------------- | -- |
| **S1** | 修 tooltip 元素 BUG：`_on_cell_hover` 改读有效元素 | 火剑的斩击悬浮显示「火 · 对敌克制 ×1.5」，与结算一致                                   | ✅  |
| **S2** | 伤害分解显示（§5.2，P0）                          | 中栏面板出现 `⚔ 斩击·火  基础8 × 连线2 × 克制×1.5 = 24` + `小计 36 × 护符×1.5 = 54` | ✅  |
| **S3** | 敌人弱点/抗性呈现（§6.3）                          | 图例改为「敌人 火 / 弱 暗 ×1.5 / 抗 冰 ×0.85」，各按元素着色                         | ✅  |
| **S4** | 符号按元素着色（§6.1）                            | 火剑的斩击带橙色边框+底色，铁剑的斩击为中性灰边                                         | ✅  |

**S2 实现决策（重要）**：分解**不走战斗日志**。`hud._log` 是 `push_front`（多行会**倒序**显示）、上限 10 条（一次分解就会冲掉全部历史）、且右栏仅 170px 宽会挤爆。改为中栏（最宽、正对转轮）独立 `PanelContainer`，每回合整体替换，`_begin_spin` / `_reset_grid` 时清空。

完成后跑 §17 验收前 4 条。

### 15.3 真机验证（阻塞项，需用户 F6）

> ⚠ 本机无 Godot 二进制，所有改动仅静态验证（括号配平 / 无残留引用）。**以下必须真机跑。**

| 验证点                               | 关注                                                                                               | 不达标时调什么                                                                                                           |
| --------------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 旋转手感                              | 高速后"明显难停但不是不可能"                                                                                  | `_SPIN_MIN_WAIT`（调大=慢=好停）、加速斜率 0.0015                                                                             |
| 槽位取舍                              | 带 1 把武器时目标符号密度 50%、好按停；带 2 把掉到 ~25%、变难                                                           | 差异不明显则调 `_SPIN_MIN_WAIT`                                                                                          |
| 护符膨胀                              | 带 `rage_charm` 后伤害明显变大                                                                           | `mult_value`                                                                                                      |
| ante 变肉                           | 中期不秒怪、也不卡关                                                                                       | `ANTE_ACT_STEP_*` / `ANTE_ROOM_STEP_*`（**需 S2 分解面板辅助**）                                                                              |
| 克制感知                              | ×1.5 爆发爽、×0.85 小亏不崩                                                                              | `MULT_ADVANTAGE` / `MULT_RESIST`                                                                                  |
| 灼烧手感                              | burn base=4 疑偏低、decay 偏快                                                                         | 待真机确认后再定                                                                                                          |
| **S2 分解面板**（新）                    | 中栏面板不遮挡转轮、3 符号时不撑破布局；数字与飘字总伤一致                                                                   | 过长则缩短行格式（去掉 `基础(=a+b)` 展开）                                                                                        |
| **S3 弱点行**（新）                     | 「弱 X / 抗 Y」与实际结算方向一致（弱 X = 带 X 打出 ×1.5）                                                          | 若方向反了则对调 `weakness`/`resists`                                                                                     |
| **S4 格子配色**（新）                    | 火剑斩击 vs 铁剑斩击可一眼分辨；非旋转期（disabled 态）配色仍在                                                           | 对比不足则调 `lerp` 权重 0.14 / 0.55、边框 3px                                                                               |
| **D1 元素 debuff**（已落地，见 §15.1 #11） | 火焰剑/冰霜枪/毒匕首/火焰法杖 的转轮出现各自元素 status 符号（火→燃/冰→霜/毒→毒）；敌人被挂霜/毒后攻击明显变小、灼烧持续掉血；匕首/铁剑等无属性武器转轮无 debuff 符号 | debuff 不出现→查武器 .tres 的 reel 是否含对应 status 符号；减益不生效→查 `_enemy_deal_damage` 的 `atk_down`                             |
| **S5 防御统一为护盾**（已落地）               | 带守备护符后每回合自动 +4 护盾（面板「护盾 N」随回合增长、挨打才掉）；守望护符仍给开局爆发护盾；无防御护符时护盾为 0，敌人攻击直扣 HP                         | 涓流不生效→查 `_apply_charms` 的 `shield` 分支与 `_agg_shield` 的 `charm_shield_trickle`；数值手感→调 `guard_charm.tres` 的 `value` |
| **S9 槽位成长**（新，已落地）                 | 整备屏四列各显初始配额 2/1/1/1；武器/技能列槽位条末尾有 `＋`（无顶）、护符列显 `◇··`（硬顶3）；商店买第 3 把武器提示「开槽 13 金」，买完武器列变 `◆◆◆＋`；任一类满槽**不影响**他类可选 | 扩槽不生效→查 `_grow_slot`/`_can_grow_slot`；价格不对→查 `_shop_price` 的 `max(0, owned-SLOT_INIT+1)*step` |
| **S9 稀释刹车**（新，核心判据）                | 带 4–5 把武器后：废铁占比明显升高、目标符号命中率下降、按停变难——**稀释是否真能替代硬顶**充当刹车 | 若带越多反而无脑变强 → 说明稀释不够，需上调废铁权重或改回硬顶（**此条决定 §7 无顶设计能否立住**） |
| **UI 尺寸**（新，`46641a9`）              | 四列在 1280 宽下不挤、右侧护符列文字不截断；卡片高 66px 不显局促、武器列 8 张卡滚动顺畅 | 仍挤→再收列宽或改 2×2 网格；卡片太扁→回调 `_make_item_card` 的 66 |
| **run 收尾 BUG 修复**（新，`333717a`）      | 完整跑到**幕三 BOSS**（21 房不中断）；幕一/幕二 BOSS 通关后正常进**商店**而非弹三选一；仅通关**最终** BOSS 才弹元进度三选一 | 仍中途重开整局→查 `_is_run_final` 是否被三处结算点调用（此前用 `reward_is_boss` 直接 `_full_reset()` 是 BUG 根因） |
| **元进度三选一持久化**（新，`4afacf2`/`333717a`） | 最终 BOSS 后仅出 **3 张**卡（非全部候选）；护符升级卡**仅**锋锐/狂怒/深渊之瞳（`effect=="damage_mult"`，守备/抗扰/丰沛/守望不出卡）；退出重进后 `weapon_base_bonus`/`charm_upgrades` 仍生效（meta 已 `_save_meta()`） | 出卡数≠3→查 `_roll_meta_choices` 的 `shuffle().slice(0, META_CHOICE_COUNT)`；持久化失效→查 `_load_meta` 默认值是否含两键 |
| **连锁重触发**（新，`afcfb29`）           | 打出 special 三连 → 免费重转且只有 special 部分吃 ×1.5/次（封顶 ×3.375）；敌人被打死立即中断连锁；伤害分解尾部显 `连锁×N`、有 ⚡ 飘字 | 触发太频繁/不够爽→调 `CHAIN_STEP`/`CHAIN_MAX`；不触发→查 `_last_special_triple` 判定 |
| **消耗品腰带模型**（已落地，见 §15.1 #20/#23/#44） | 固定 4 格腰带 2x2 网格（ReelDock 左侧，参考右栏虚线格子图），整备初始 1 格 + 商店最多补 3 格；4 cell 固定在 HUD，charges 用完/卖出后槽位自动变空（不增删 cell）；净化上限整条线已废（净化完全走消耗品·净化药剂，按下格子直接用） | 重复购买失败→查 `_on_shop_buy_pressed` 的 `consumable_slots.append` 与 `CONSUMABLE_CAP` 判定；卖出错格→查 `_on_shop_sell_pressed` 的 `target` 索引；用尽不移除→查 `_on_consumable_pressed` 的 `slot["charges"] <= 0` 分支；净化药剂点击无反应→查 4 cell 的 `pressed.connect` 与 `game_state`/`in_loadout` 守卫 |
| **S12 商店金币升级 tab**（新，`4d6b931`） | 三页签切换正常（购入/卖出/升级互不遮挡）；升级页 4 张卡显 Lv/效果/价格；锋锐研磨→伤害数字变大、连线精通→2连显示×3且伤害涨、护符共鸣→伤害分解尾部显「共鸣×N」且总伤放大、壁垒→进下一房护盾更高；金币不足/满级时按钮禁用；离开商店进下一房升级仍生效；新一局（元进度三选一后）升级清零 | 不生效→查 `_gold_upgrade_defs`/`_on_gold_upgrade_pressed` 与 `_agg_*` 接入；共鸣不显示→查 `_evaluate` 尾项 `jf`；连线倍率误伤单符号→查 `_contribute` 的 `raw>=2` 闸门 |

> ⚠ 上述验证点**尚未真机执行**。代码已全部推送到 `origin/master`（HTTPS `https://github.com/ChrisUtada/seek_dead.git`，最新 `3f8aefb`，见 §15.1 提交历史表第 11–35 行）。回退基线 `.bak` 已删，故发现问题只能 `git revert` 整提交。验证仍建议尽早 F6 跑一遍 §15.3 表格。

### 15.4 阶段 2：经济与防御（4 项，已随 9926365 提交）

| 步骤     | 任务            | 说明                                                                                                                                  |
| ------ | ------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **S5** | 防御统一为护盾 ✅ 已落地 | 删 `armor` 数值；`guard_charm` 改每回合护盾涓流（`_agg_shield` + `charm_shield_trickle`），`受伤 = max(0, raw − shield)` 接入 `_enemy_deal_damage`（§8） |
| **S6** | 局内金币 ✅ 已落地    | `gold` 变量（起始4，每局清零）；`_award_gold` 清房+5/Boss+10；转轮金币符号 `gold.tres` 常驻池（落线产金、不造成伤害）（§11）                                              |
| **S7** | 商店 ✅ 已落地      | 奖励后入商店覆盖层；`candidates.shuffle()` 随机刷新货架（武器/护符/物品/技能），分类定价，购买写入 selected 数组、下房 `_start_room` 生效（§11）                                 |
| **S8** | 利息 ✅ 已落地      | `_award_gold` 并入 `min(gold//5, 5)`，每5金+1上限+5（§11）                                                                                   |

### 15.5 阶段 3：结构与长尾（3 项）

| 步骤      | 任务       | 状态        | 说明                                                                                                |
| ------- | -------- | --------- | ------------------------------------------------------------------------------------------------- |
| **S9**  | 槽位成长（四类统一） | ✅ 已落地 | **初始配额 2/1/1/1、天花板 ∞/∞/4/3**（进池类武器·技能 `UNCAPPED` 无顶，不进池类硬顶；消耗品为固定 4 格腰带、不再「买即开槽」成长），四类统一「买即开槽」（`_grow_slot`，仅武器/技能/护符）；`_shop_price` 按类步进（武器+5/技能+6/护符+8/消耗品+4），加价从「首次扩槽」起算（`max(0, owned-SLOT_INIT+1)*step`）；四类互不算总、无全局总槽；旧「槽扩展」商品方案废弃 |
| **S10** | run 结构扩容 | ✅ 已落地（含 T2/T3；2026-08-09 房数重排待代码同步） | **21 房 = 3 幕 ×（4 普通 + 2 精英 + 1 BOSS）**，每局 `_full_reset` 时 `_build_run` 从全量 51 间 .tres 池按幕抽取（每幕 4 普通 + 2 精英 + BOSS 4 选 1，BOSS 池随内容补齐至 12）。`RoomData` 已含 `kind`(normal/elite/boss) + `act` + `gimmick_script`；`_sort_rooms` 语义重排修旧字母序难度倒挂；`_is_boss_room(idx)` 按 kind 判定 BOSS；`_is_run_final(idx)`（idx≥ROOMS.size()-1）判定通关整局（**与是否 BOSS 房分离**，见 §15.9 #1）。**T2 BOSS 机制**：`boss_gimmick.gd` 基类 + 三 BOSS 子类（熔铸护甲 / 呓语锁轮 / 深渊侵蚀），钩子 `on_room_start/on_turn_begin/on_damaged/on_special_triple` 由 `_start_room` 经 `RoomData.gimmick_script.new()` 实例化。**T3 精英房**：数值介于普通与 BOSS 之间；**T6 精英奖励已落地**——专属「战前补给」三选一（金币囤+18 / 铁砧点+2 / 结界备战+30护盾），见 §15.9 #5。**待办（见 §15.9）**：三 BOSS 机制改为 counter-build 核爆解法（②）。⚠ 单局时长须权衡：21 房 × 手动停 3 列，约 40–50 分钟 |
| **S11** | 内容广度     | 🔄 基础量已就位 | 现有：武器 12 / 护符 10 / 技能 4 / 消耗品 4 / 符号 19；目标更多武器/护符/敌人/房间/事件（§12.4：长尾靠内容不靠系统）                             |

### 15.6 随手清理

- `_affix_*` 5 个中性占位函数（词缀已废弃，流程零调用）→ **已删除**（F-0 死代码清理，提交 `620de1d`）。配套删除 `scripts/equipment/enums.gd`、`stat_modifier.gd`，及 `weapon_data.gd` 死代码段（见 §15.1 #15）。

### 15.7 不排期

冻结清单（§13）不排期。需要时**逐个解冻、单独评估**，不整批解冻。

### 15.8 推荐推进顺序（待用户拍板）

当前 MVP 战斗闭环已成型（攻击三乘区 + 元素 debuff + 手动停转轮），**经济层也已落地**（S5–S8：防御统一为护盾 / 金币 / 商店 / 利息）。推荐顺序：

1. **（阻塞）F6 真机验证** §15.3 全部点 —— 任何数值手感（旋转速度 / 克制 ×1.5 / ante）都需实机确认，否则后续调参无锚点。
2. **S5 防御统一为护盾**（✅ 已落地）—— 最小、独立的防御补强，把"挨打"的稳定减伤预期收进单一护盾资源（守备护符=每回合涓流），不引入新数值轴（贴合 3 乘区设计）。落地：`charm_shield_trickle` + `_agg_shield()`。
3. **S6–S8 局内经济**（✅ 已落地）—— 金币（`gold` 起始4每局清零 + `_award_gold` 清房+5/Boss+10）+ 商店（`_roll_shop` 随机刷新货架、分类定价、购买写入 selected 数组、下房生效）+ 利息（`min(gold//5,5)`，每5金+1上限+5）。给 run 真实 stakes 与 build 调整窗口（铁砧点回收、买护符/武器）。
4. **S9 槽位成长**（✅ 已落地）—— 四类初始配额 2/1/1/1 + 统一「买即开槽」；天花板按「进池与否」分野（武器/技能无顶靠稀释+递增价刹车，消耗品/护符硬限）；整备屏四列分区 + 槽位条。
5. **S10 结构长尾（✅ 已落地）** —— 21 房 run 结构（每局从全量 51 间 .tres 池按幕抽取：每幕 4 普通 + 2 精英 + BOSS 4 选 1）、BOSS 机制 T2（三 BOSS 子类）、精英房 T3。**S11 内容广度** —— 更多武器/护符/敌人/房间（见 §15.9 #7）。
6. **F-0 死代码清理** —— **已完成**：删 `_affix_*` 5 个中性占位 + `equipment/enums.gd`、`stat_modifier.gd`、以及 `weapon_data.gd` 死代码段。

> 注：S5 之前若想先验证"防御手感"，可临时把某房敌人 atk 调高观察；但正式实现应在 S5 而非堆敌人数值。

---

### 15.9 当前待办 / 未开工清单（务必不遗漏）

> 本节为**唯一权威待办台账**，每次回写时同步勾除已完成项。状态：🔴 阻塞（须先做）/ 🟡 进行中候选 / ⚪ 未开工 / 🔵 用户明确延后 / 🟢 吸收进 F6（取消独立阶段，并入真机调参）。

| # | 待办 | 状态 | 关联 | 说明 / 下一步 |
| - | ---- | ---- | ---- | ---- |
| 1 | **F6 真机验证**（§15.3 全表，含腰带模型新增 4 项） | 🟢 已真机执行（2026-08-03 用户手动测试） | §15.3 | **⚠ 测试时 `TEST_PLAYER_DMG_MULT=1.5` / `TEST_STATUS_DMG_MULT=3.0` 调试倍率仍开启**（`duel_controller.gd:92-93`）——玩家直击 ×1.5、状态 DoT ×3.0，手感/难度判断被放大失真。结论须待改回 `1.0` 后复测才可信，尤其 #6 ante α 调参不能直接用本次数据。Priority 验证「完整跑到幕三 BOSS」据用户口述已通过。**（用户 2026-08-03 决定：先保留调试倍率，待实测反馈 + α 定稿后一并回退并终测）**§15.3 末行「消耗品腰带模型」4 项验证点对应提交 `a37072c`/`388bbad`（整备上限 1、腰带容量 4） |
| 2 | **② 反制即爆发（Plan C）✅ 已落地（F6 已覆盖，待据真实倍率复测）** | 🟢 已落地（F6 已覆盖） | §10.6 | 用户原话「想有特别爆炸的解法」。落地：**敌人护甲扁平池（先破甲后掉血）+ 穿透符号（shot/pistol_shot/ice_shot）**；反制双触发——`special` 三连 = 通用破甲（开直击HP窗口）、克制元素三连 = 进阶核爆（额外 20% max-HP 穿透）。锈蚀傀儡熔铸护甲改为成长型护甲池（`ARMOR_PER_STACK=8`，上限3层），旧 `boss_dmg_mult` 减伤已废弃。解决"无暗符号"卡点：`special` 三连作通用兜底破甲。见 §10.6。⚠ 上述核爆/破甲体验在 `TEST_*_MULT` 开启下被放大，改回 1.0 后需复测手感是否仍成立 |
| 3 | **③ 商店金币升级 tab（P3 → 已落地 S12）** | 🟢 已落地（F6 已覆盖） | §10.1 / §11 / S12 | 用户原话「应该有一个可以用金币升级的房间」。与 meta 三选一分工：meta 层持久（每局结束三选一）、**P3 局内金币升级**走商店新增 tab。方案定稿：**深化已有 3 乘区、不新增第 4 乘区**——锋锐研磨(基础)/连线精通(连线倍率)/护符共鸣(×1+N·0.25 并入 buff_mult)/壁垒(开局护盾)；`gold_upgrades` 局内状态每局清零。提交 `4d6b931`，验证点见 §15.3 末「S12」行。⚠ 升级数值（尤其锋锐研磨基础伤害、护符共鸣乘数）在 `TEST_*_MULT` 失真下评估，改回 1.0 后需复测性价比 |
| 4 | **单一护盾防御（Phase G 阶段 2 实质收尾）** | 🟢 吸收进 F6 | §8 | **2026-08-03 用户拍板取消独立阶段**：S5 已 100% 落地单一护盾资源模型（开局爆发护符 + 每回合涓流护符 + 奖励守望结界 + 铁壁护符 + 转轮护盾符号全部接线，无悬空 effect/id），设计目标已达成。剩余「护符/奖励/技能灌入护盾的全局节奏与时机细化」本质是数值调参、无真机锚点，**并入 #1 F6 真机验证批次**一并定夺（含护盾是否需上限/衰减的判定）。原「2026-08-02 延后考虑」升级为「不再单列阶段」 |
| 5 | **精英房奖励待遇细化（T6）** | 🟢 已落地（待 F6） | §15.5 S10 | 精英房差异化：专属「战前补给」三选一（金币囤+18 / 铁砧点+2 / 结界备战+30护盾），`resources/rewards/elite/` + `ELITE_REWARD_POOL` + HUD `_show_reward_screen` 增 elite 分支 + `_apply_reward` 三分支。普通房仍走 `REWARD_POOL`、BOSS 走武器池，三档节奏理顺（普通小增益→精英补给决策→BOSS战力飞跃）。见 §15.5 S10 T6 |
| 6 | **ante 曲线 α 调参（可能需下调）** | 🟡 待 F6 | §10.2 | 原设计只跑到 4 房（BUG 致）；现真跑满 21 房（幕内 ria 0–6），血量曲线很可能偏陡。F6 实测后下调 α_hp/α_atk（见 §16） |
| 7 | **S11 内容广度扩展** | 🟡 基础量已就位 | §12.4 | 现有武器12/护符10/技能4/消耗品4/符号19（`549e9b0` 补光暗 4 武器后）；目标更多武器/护符/敌人/房间/事件（长尾靠内容不靠系统） |
| 8 | **④ 元进度三选一候选池扩容（可选）** | ⚪ 未开工 | §10.5 | 当前候选仅为「本局携带武器 + 乘区型护符 + 铁砧」；未来可加入全局未持有武器/护符卡，增强 meta 成长面。非阻塞 |

> 推进顺序建议：**先 #1 F6**（跑通 21 房 + 三选一持久化 + 连锁手感）→ **#6 ante α 调参**（基于 F6 实测）→ 其余按资源排期。**#2 反制即爆发（Plan C）已落地（§10.6，待 F6）**；**#3 商店金币升级 tab 已落地（S12，待 F6 验证）**。

---

## 16. 待拍板 / 调参项

| 项          | 当前值                    | 推荐/规划                         | 说明                                |
| ---------- | ---------------------- | ----------------------------- | --------------------------------- |
| 转轮列数       | `REELS=3`、`ROWS=1`     | **维持 3 列**                    | 4 列已撤销（§10.3）；若要更多格子优先考虑 `ROWS=2` |
| 武器槽        | 初始 2                | **无天花板（`UNCAPPED`）**           | 买武器直接开槽；废铁/稀释自然刹车 + 递增价（8·8·13·18·23·28…）保住张力与按停密度 |
| 技能(skill)槽   | 初始 1                 | **无天花板（`UNCAPPED`）**           | 与武器同为进池类，同一把尺子；步进最陡（6·12·18·24…）因挤占转轮带最凶 |
| 护符槽        | 初始 1                 | **硬天花板 `CHARM_CAP=3`**           | 不进池、零代价、无自然刹车故须硬限；唯一收集乘区，步进价最贵（10·18·26）保稀缺 |
| 消耗品槽      | 初始 1                    | **硬天花板 `CONSUMABLE_CAP=4`（固定 4 格腰带，可重复购买同类，不随购买成长）**       | 不进池，仅界面负担，宽松硬限                  |
| 克制倍率       | ×1.5 / ×0.85 / ×1.0    | 维持                            | ×0.85 温和，可真机微调                    |
| 敌元素字段      | 单 `element`            | 维持                            | 直觉问题由"弱点/抗性呈现"解决，双字段延后            |
| ante 初值    | α_hp=0.15 / β_atk=0.10 | **F6 后很可能需下调**                  | 现真跑满 21 房（idx 0–20，幕内 ria 0–6），血量曲线在原 4 房设计下偏陡；**依赖 S2 分解面板 + F6 实测**（见 §15.9 #6） |
| 旋转速度       | 0.09 → 0.035s          | 待真机调                          | 主难度旋钮，见 §4.4                      |
| 经济初值       | 已落地（静态未真机）             | 起始4金/清房+5/Boss+10/利息每5金+1上限+5 | 需真机调手感                            |
| run 长度     | 21 房（3 幕 × 4普通+2精英+1BOSS） | 维持（代码待同步）          | 每局 `_build_run` 从全量 51 间 .tres 池按幕抽取：4 普通 + 2 精英 + BOSS 4 选 1；单局约 40–50 分钟（手动停 3 列 × 21 房） |

---

## 17. 验收标准

1. 带护符后伤害**明显膨胀**，敌人随 ante **同步变肉**（中期不秒怪）。
2. 玩家能**心算每个大数字的来源**（连线/护符/克制，伤害分解可查）。
3. **惩罚温和可控**：带错元素仅 ×0.85 小亏、不崩盘，中性武器提供对冲退路；玩家凭弱点/抗性信息决策而非背环。
4. build 决策**可见地影响**伤害（换元素覆盖/配对护符 → 数字变化）。
5. **按停技能层成立**（v2.1 新增）：高速下停在目标符号上"难但可能"；且**带 1 把武器明显比带 2 把好按**——这条手感差异是槽位设计能否立住的判据。
6. F6 真机跑通核心循环：整备 → 转轮(手动停) → 结算(分解) → 奖励 → 商店 → 更深房。

---

> 本框架为 v2.1（v2.0 极简爽感收敛版 + 方案 A 技能层 + 槽位收紧）。核心立场：**爽感靠"3 个看得懂的乘区 + 线性×乘法双轨"，策略靠"build 层的有意义取舍 + 按停技能层"，复杂度靠"砍平行系统、呈现层降负、内容广度撑长尾"。**
>
> **维护约定**：每次落地改动后回写 §15 台账与 §14 状态列；机制模型发生变更时，**必须复查依赖旧模型的旧结论是否失效**（REELS 3→4 就是一次典型的失效案例）。

