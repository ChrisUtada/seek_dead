# Seek Dead（寻死者）— 项目概览

> 目的：让不了解本项目的人能在 15 分钟内看懂游戏现在**是什么**、**做到哪了**、**关键设计约束与待办**。
> 状态基准：截至 2026-08-06 的 `master`（提交 `c7bd8d2`）。文档描述以实际代码 / 资源为准，标注「提案」的为尚未落地的设计文档。

---

## 1. 一句话定位

**俯视角回合制「老虎机肉鸽」**：玩家靠转动一行三格的转轮产出攻击符号，靠「符号匹配 / 元素克制 / 破甲 / 状态」击败敌人；局间用金币在商店强化、用铁砧点数跨局抽装备，逐局逼近三幕 BOSS。本质是**「克制解谜」肉鸽**——敌人机制先于装备设计，装备围绕如何解机制展开。

- 技术栈：**Godot 4.7（纯 GDScript，无 C#）**
- 入口场景：`scenes/duel/duel.tscn`（F6 直接跑）
- 主控制器：`scripts/battle/duel_controller.gd`（状态机 + 全代码 UI 驱动）
- 美术：占位像素风 + Kenney UI 素材包（本地 `assets/`，尚未入库）

---

## 2. 一局的结构（核心循环）

```
整备(Loadout) → 对决(Duel, 转轮) → 房奖励(三选一) → 间幕(可选商店)
      ↑                                                  │
      └──────── 死亡/通关后回整备 ──────────────────────┘
通关最终 BOSS 后 → 铁砧(Anvil) 抽装备 → 下一局更富
```

- 一局 = **12 房**（每幕：2 普通 + 1 精英 + 1 BOSS）× 3 幕。
- **仅通关最终 BOSS** 才弹元进度（铁砧抽装备三选一前的持久化）。
- 死亡 → 回到整备页面（不重开本房，提交 `c7bd8d2`）。

---

## 3. 战斗系统

### 3.1 转轮
- 1 行 **3 格转轮**，玩家按 SPIN 转动，**何时停由玩家决定**（无自动停止上限）。
- 符号池 = 合并「武器 reel + 技能符号 + 金币符号」。
- 单符号必结算；**同符号匹配有连线倍率**（连线精通升级影响此轴）。

### 3.2 伤害结算（乘区）
```
total = (连线伤害 + special×连锁倍率) × 强袭 × 增益乘区 × 测试倍率
增益乘区 = 护符伤害乘区 × 状态/暴击等
逐符号再 × 元素克制系数
```
- 护甲是**扁平池**：先破甲再掉血；`pierce_armor` 绕过；special 三连清空护甲。
- 关键常量：`BASE_POWER_REF=32`（武器伤害归一支点）、`CHAIN_STEP=1.5`/`CHAIN_MAX=4`（special 三连连锁重转）、`TEST_PLAYER_DMG_MULT=1.5` / `TEST_STATUS_DMG_MULT=3.0`（手感验证保留值，正式平衡可改回 1.0）。

### 3.3 元素克制（五元环）
```
火 > 冰 > 毒 > 光 > 暗 > 火
克制 ×1.5 / 抵抗 ×0.85 / 无 ×1.0，单向 玩家→敌人
```
⚠️ **头号设计约束**：武器目前只有 `fire / ice / poison` 三种元素，**没有 light / dark 武器**。因此 Act2（暗 BOSS）、Act3（光 BOSS）**当前无法被元素克制**，只能靠中性伤害 / 破甲 / 状态硬磨。详见 §7 待办。

### 3.4 状态（DoT）
`灼烧(burn) / 霜冻(frost) / 毒(poison)`，每回合结算，受 `TEST_STATUS_DMG_MULT` 放大。

### 3.5 敌人意图
敌人每回合声明意图（普攻 / jam 锁轮 / lock / chaos / heavy 等），玩家需针对性应对（抗干扰护符、`purify` 净化药剂、破甲）。

---

## 4. 装备与收集（整备四类槽）

整备槽位初始：`weapon 2 / skill 1 / active 1 / passive 1`。护符槽天花板 `CHARM_CAP=3`，消耗品腰带 `CONSUMABLE_CAP=4`。

### 4.1 武器（8 把模板）`resources/weapon_templates/`
| 武器 | 元素 | base_power |
|---|---|---|
| 火焰剑 fire_sword | fire | 45 |
| 烈焰法杖 flame_staff | fire | 40 |
| 战斧 battle_axe | — | 50 |
| 铁剑 iron_sword | — | 35 |
| 冰枪 ice_gun | ice | 30 |
| 毒匕 poison_dagger | poison | 18 |
| 匕首 dagger | — | 25 |
| 手枪 pistol | — | 20 |

> 武器是「符号 reel + 基础伤害」的载体；元素决定克制轴。非元素武器靠高 base_power / 暴击补位。

### 4.2 护符（10 个）`resources/charms/`
| 护符 | 图标 | effect | 数值 |
|---|---|---|---|
| 锋锐 sharp_charm | 🔥 | damage_mult | ×1.10 |
| 狂怒 rage_charm | ⚡ | damage_mult | ×1.50 |
| 深渊之瞳 abyss_relic | 👁 | damage_mult | ×1.15 |
| 守备 guard_charm | 🛡️ | shield | 4/回合 |
| 铁壁 bulwark_charm | 🧱 | shield | 8/回合 |
| 守望 ward_charm | 🛡️ | room_shield | 12/房 |
| 回春 regen_charm | 🌿 | heal | 6 |
| 抗扰 resist_charm | 🚫 | interference_resist | 1 |
| 呓语残页 whisper_relic | 🔮 | interference_resist | 2 |
| 锈蚀核心 rust_relic | 🛡 | damage_bonus | 4 |

> 护符等级系统已存在（铁砧 `charm_upgrades` 字典），但**目前仅 `damage_mult` 类护符的等级生效**——非伤害护符的等级通道尚未打通（待办 §7）。

### 4.3 技能 / 增益（4 个）`resources/skills/`
> 代码里原名「增益(buff)」，Phase C 重构改名「技能(skill)」，本质是在局内生效的主动效果。`selected_skills` 持有，HUD 左栏 `增益` 行显示图标。

| 技能 | 图标 | 说明 |
|---|---|---|
| 迅捷印记 haste | ⚡ | 提速 / 行动相关 |
| 奥术飞弹 magic_bolt | ✦ | 额外符号伤害 |
| 狂怒印记 rage | 💢 | 伤害增益 |
| 治疗术 recovery | ✚ | 回复生命 |

### 4.4 消耗品（4 个）`resources/consumables/`
腰带 4 格（2×2），每格独立持有、允许同类重复占格，按 `uid` 精准使用 / 卖出。

| 消耗品 | 效果 | 说明 |
|---|---|---|
| 强袭药剂 assault_potion | assault ×2 | 下次转轮所有伤害符号翻倍 |
| 治疗药剂 heal_potion | heal 30 | 立即回血 |
| 净化药剂 purify_potion | purify | 清除敌人干扰意图 |
| 重转卷轴 reroll_scroll | reroll | 免费重转一次（不触发敌人回合） |

---

## 5. 敌人与 BOSS

共 **21 个房间资源**，分 3 幕，类型 `normal / elite / boss`。

### 5.1 三幕 BOSS（机制原型）
| 幕 | BOSS | 元素 | HP/ATK | 护甲 | 机制 gimmick |
|---|---|---|---|---|---|
| Act1 | 锈蚀傀儡 rusty_golem | ice | 175/15 | 20 | 熔铸护甲 rust_armor（每 2 回合 +1 层，叠加 RoomData.armor） |
| Act2 | 呓语教徒 whispering_cultist | dark | 290/20 | 25 | 呓语锁轮 whisper_lock（锁轮 / 强化敌人伤害） |
| Act3 | 深渊监视者 abyss_watcher | light | 460/27 | 35 | 深渊侵蚀 abyss_erosion（注入废铁格干扰转轮） |

### 5.2 「衍生产品」课程化模型（设计原则）
一幕 = **先教分散子机制，最后 BOSS 合成**：
- **普通 A**：同源元素、无 gimmick（如 Act1 锈盲蝠 = 纯冰，同 BOSS 家族但没长全护甲）
- **普通 B**：异源 / 变奏，避免单调
- **精英**：同源元素 + BOSS gimmick 弱化版
- **BOSS**：元素 + 完整 gimmick + 全意图

> 该模型在数据结构上已半成立（锈盲蝠就是「没长全的锈蚀傀儡」），但普通 / 精英房尚未全部接入规范。

### 5.3 元素分布（实测）
- ice 敌：锈蚀傀儡、锈盲蝠、酸蚀傀儡、亵渎书吏、裂石像（light? 实际裂石像是 light，见下表）
- dark 敌：呓语教徒、暗影信徒、虚空回响、虚空低语者、深渊咏者
- fire 敌：湮灭使徒/领主、迷魂灯笼
- poison 敌：邪颂祭司、腐化史莱姆、腐殖巨蠕、毒孢菌、裂界游魂
- light 敌：深渊监视者、坍缩星核、裂石像、终焉守望

gimmick 脚本共 4 个：`rust_armor` / `whisper_lock` / `abyss_erosion` / `boss`（基类）。

---

## 6. 元进度与货币

两套货币，**生命周期完全不同**：

| 货币 | 性质 | 来源 | 去处 |
|---|---|---|---|
| **金币 gold** | 每局清零 | 清房 / 精英 +18 / 卖出 / 连击结算 | 局内商店（升级 + 买/卖/升级物品） |
| **铁砧点数 anvil_points** | 跨局永久 | 通关（首通奖励 first_clears） | 铁砧老虎机抽装备 |

### 6.1 局内商店（S12，金币）
升级页 4 项（每局清零、独立于护符）：

| 升级 | 图标 | 效果 | 价格 | 上限 |
|---|---|---|---|---|
| 锋锐研磨 power | 🗡 | 攻击力 +3/级 | 6+4·级 | 6 |
| 连线精通 line | 🔗 | 连线倍率 +1/级 | 8+6·级 | 4 |
| 护符共鸣 joker | 🔮 | 护符乘区 ×(1+0.25·级) | 12+8·级 | 4 |
| 壁垒 shield | 🛡 | 每房护盾 +5/级 | 5+3·级 | 5 |

> **提案**：把 S12 从「硬写 4 项」重构为「数据驱动升级轨道表」，让每条升级 bind 到真实来源（武器 / 护符 / 卷轴 / 训练），详见 `docs/商店升级重构_执行方案.md`。尚未落地。

### 6.2 铁砧老虎机（anvil，铁砧点数）✅ 已落地
- 界面 `anvil_screen.tscn/.gd`（提交 `c393976` 起的迭代）。
- **纯 gacha**：消耗 `ANVIL_ROLL_COST=10` 点摇一次，**三格必出同一件装备**（仪式感三连 + 问号悬念 + 错峰停格）。
- 落点注入 `meta["owned_weapons / owned_charms / owned_consumables"]` 拥有池；新装备自动进整备备选。
- **重复处理**：摇到已拥有 → 升该物品永久等级（`charm_upgrades` / `weapon_base_bonus`），永不浪费；封顶时返还点数兜底。
- **保底**：`ANVIL_PITY_MAX=10` 连续重复后，下次必出未拥有。
- **图鉴收集**：`collection_milestones` 记录收集度。
- 摇出的装备**不含消耗品**（修复于 `691dfd4`）。

---

## 7. 当前关键设计决策与待办

### 已定方向
1. **BOSS 先行 + 锁步**：先定敌人机制原型，再补能解它的装备（见 `docs/BOSS_设计_执行方案.md`）。
2. **元素词汇表须先于 BOSS 元素设计完备**（铁律）。
3. **铁砧 = 跨局抽装备的纯 gacha**；**商店 = 每局强化手上装备**。双系统边界清晰。
4. **净化完全走消耗品**（净化药剂），无独立净化按钮。

### 开放 / 阻塞项（按阻塞顺序）
1. **🔴 补 light / dark 武器**：否则 Act2、Act3 的 BOSS 无法被元素克制（当前只能中性/破甲/状态硬磨）。这是放开后两幕设计的前置。
2. **🟠 普通 / 精英房接入课程化规范**：让「衍生产品」模型覆盖全 21 房。
3. **🟠 铁砧等级扩展到非伤害护符**：`charm_upgrades` 等级现仅 `damage_mult` 生效，需扩到 shield/room_shield/heal/interference_resist/damage_bonus。
4. **🟡 护符设计三项缺口**（见 BOSS 文档 §6）：破甲护符、元素优势增幅护符、状态护符。
5. **🟡 S12 商店升级重构**（数据驱动轨道表）— 提案，未落地。
6. **🟢 视觉细化**：消耗品格子虚线边框（StyleBoxFlat dash pattern）、Kenney 素材正式入库。

---

## 8. UI 现状
- 主 HUD：`scenes/ui/battle_hud.tscn` + `battle_hud.gd`（全代码构建）。
- 左栏 `PlayerPanel`：武器图标行 + 护符图标行 + 增益图标行 + 消耗品 4 格 + 血量/护盾/增益标签。
- 右侧 `EnemyPanel`：敌人名 / BOSS 徽章 / HP / 护甲 / 意图 / 状态。
- 转轮在战斗画面正下方居中（含 SPIN + 4 格消耗品）。
- 覆盖层（loadout / reward / meta / shop / anvil）各自独立 tscn+gd，`configure(controller, hud)` 注入。
- ⚠️ **Godot 写 .tscn 铁律**：绝不用文本给实例写覆盖子节点（会被保存时剥离导致 `@onready` 取 null 崩溃）；面板均已改为内联。
- 📋 **规划中（未落地）· 复古掌机化 + 整备页 5 小人**：详见 `整备页_5小人_复古掌机_执行方案.md`——把视口降到 480×270（nearest 像素缩放、aspect=keep）、像素字体 + Palette 硬边化；整备页四类目 + 铁砧改造成 5 个像素小人站（`assets/{wq,jn,xh,hf,tz}.png`），点击展开可收起右侧副屏抽屉；掌机外壳保留但屏内不画虚拟按键。分阶段 P1~P4，尚未动代码。

---

## 9. 文件结构速览
```
scenes/
  duel/duel.tscn            入口（F6）
  ui/  battle_hud / loadout / reward / shop / anvil / meta  (.tscn+.gd)
scripts/
  battle/duel_controller.gd 主控制器（状态机+结算+经济+铁砧）
         battle_math.gd      纯结算函数（不碰 HUD）
         element_counter.gd  元素五元环
         gimmicks/           4 个 BOSS 机制脚本
  ui/    *hud / *screen.gd
  items/ symbol_data/item_data/weapon_data/skill_data/reward_data.gd
  equipment/weapon_data.gd
resources/
  weapon_templates/ (8)  charms/ (10)  skills/ (4)  consumables/ (4)  rooms/ (21)  rewards/  （目标规模见 §12 / 《装备收集规划_200》：装备 200=武器90/护符45/技能40/消耗品25；敌人 60）
docs/   16 份设计/执行文档（含本项目引用的 3 份方案）
```

---

## 10. 如何运行 / 接手
1. 用 Godot 4.7 打开 `project.godot`。
2. F6 跑 `scenes/duel/duel.tscn`（无头测试由人工执行，AI 不跑）。
3. 调平衡看 `duel_controller.gd` 顶部 `@export` 常量（`BASE_POWER_REF` / `CHAIN_*` / `TEST_*` / `ANVIL_*`）。
4. 加内容：武器改 `resources/weapon_templates/*.tres`；敌人改 `resources/rooms/*.tres` + 必要 gimmick。
5. 提交约定：每阶段 1 commit + 静态校验（`verify_overlays.py` + gd 括号配平）；推送临时切 SSH 后还原 HTTPS；`.workbuddy/` 不入库。
```

---

## 11. 计划新增内容清单（含数量）

> 下表把 §7 的开放项**量化成可验收的内容条目**，与 `docs/BOSS_设计_执行方案.md` §8 缺口一一对应。单位为「新增资源文件数 / 改造点」。

### 11.1 🔴 武器（补元素覆盖缺口）— 最小 2 / 推荐 4
| 元素 | 新增数量 | 说明 |
|---|---|---|
| light（光） | ≥1（推荐 2） | Act2 暗 BOSS(呓语教徒) 弱光，须有光武器可克制 |
| dark（暗） | ≥1（推荐 2） | Act3 光 BOSS(深渊监视者) 弱暗，须有暗武器可克制 |

- **最小可行**：1 光 + 1 暗（共 **2 把**），立即让 Act2/3 BOSS 有元素解。
- **推荐**：各 2 把（一把高 base_power 近战 + 一把法杖/远程，给构筑选择），共 **4 把**新武器模板（`resources/weapon_templates/*.tres`）。
- 武器当前只有 `fire(2)/ice(1)/poison(1)`，新增后元素分布趋于完整（五元环 fire>ice>poison>light>dark>fire）。

### 11.2 🟡 护符三项缺口 — 共 3 个新护符
来源：`BOSS_设计_执行方案.md` §6 克制矩阵缺口。

| 新护符 | 建议 effect | 作用 | 缺口填补 |
|---|---|---|---|
| 破甲护符 | 破甲·穿透（special 符号权重 / 直击穿透） | 补「破甲轴」无护符的空白 | 克制矩阵 §59 |
| 元素优势增幅护符 | 放大克制 ×1.5 → ×1.7 | 强化元素克制收益 | 克制矩阵 §60 |
| 状态护符 | 状态持续/伤害加成 | 补「状态叠加轴」无护符的空白 | 克制矩阵 §62 |

> 合计 **3 个新护符**（落 `resources/charms/` + `ItemData.effect`）。落地后需同步打通铁砧 `charm_upgrades` 等级通道（见 11.4）。

### 11.3 🟠 普通 / 精英房课程化复核 — 18 个非 BOSS 房
- 21 房含 3 BOSS，其余 **18 个非 BOSS 房**需接入「衍生产品」课程化规范（普通 A 教元素 / 普通 B 异源 / 精英同源+mini-gimmick）。
- 按幕拆分：**Act1 非 BOSS 6 房 / Act2 非 BOSS 6 房 / Act3 非 BOSS 6 房**。
- 此项为**设计复核 + 字段微调**（非新增资源），但每幕落地前提是该幕 BOSS 元素克制武器已存在（受 11.1 阻塞）。

### 11.4 🟠 铁砧等级通道扩展 — 代码改造（无新增资源）
- 改造 `duel_controller.gd` `_apply_charms()`：把 `meta["charm_upgrades"]` 等级从仅 `damage_mult` 生效，扩展到 **5 类 effect**：
  `shield / room_shield / heal / interference_resist / damage_bonus`。
- 配套修 `_roll_meta_choices()`（当前主动跳过非 `damage_mult` 护符）。
- 不新增文件，但 11.2 的 3 个新护符依赖此项才能被铁砧升阶。

### 11.5 🟡 商店升级重构（S12）— 结构改造（无新增内容）
- 把 `GOLD_UPGRADE_DEFS` 的 4 项硬写，重构为**数据驱动升级轨道表**（见 `docs/商店升级重构_执行方案.md`）。
- 预置轨道：**护符强化 / 卷轴掌握 / 训练** 3 条（可选保留 `line` 通用项）。
- 无新增资源文件，是结算分发器 + 商店 UI 通用化的改造。

### 11.6 🟢 视觉与素材 — 2 类
| 项 | 内容 | 数量 |
|---|---|---|
| 消耗品格子虚线边框 | StyleBoxFlat dash pattern 细化 | 4 格样式 |
| Kenney 素材入库 | `assets/kenney_ui-pack-pixel-adventure/` 正式提交 | 1 批（当前未入库） |

### 11.7 本节（§11）暂不动的类别
> 注：以下仅限「§11 的最小解阻塞增量」范围；其完整 v1.0 目标量见 §12（依《装备收集规划_200》）：技能 **40** / 消耗品 **25**。
- **技能**：当前 4 个，§11 的解阻塞增量里未单列 → 本节不加（长期纳入 200 计划）。
- **消耗品**：当前 4 个，铁砧已明确不出消耗品 → 本节不加（长期纳入 200 计划）。

### 11.8 汇总（数量速览）
| 类别 | 新增数量 | 阻塞级别 |
|---|---|---|
| 武器（light/dark） | 最小 2 / 推荐 4 | 🔴 |
| 护符缺口 | 3 | 🟡（依赖 11.4） |
| 非 BOSS 房复核 | 18（改造非新增） | 🟠（依赖 11.1） |
| 铁砧等级通道 | 0 文件 / 1 改造 | 🟠 |
| 商店升级重构 | 0 文件 / 1 改造 | 🟡 |
| 视觉/素材 | 4 格样式 + 1 批素材 | 🟢 |
| **内容资源合计（含推荐）** | **约 7 个新 .tres**（4 武器 + 3 护符） | — |

---

## 12. 最终目标数量（内容规模 — 依《装备收集规划_200》）

> 本节数字**全部来自 `docs/装备收集规划_200.md`**（200 件可收集装备分配方案 + 敌人 60 规划），是该文档的浓缩索引，避免两处数字打架。
> 视角区别：**§11** = 立刻解阻塞的「最小增量」；**§12** = v1.0 完整内容规模（200 装备 + 60 敌人），分批里程碑推进，不阻塞玩法开发。

### 12.1 主表（当前 → 最终目标）
| 类别 | 当前 | 最终目标 | 增量 | 来源（200 文档） |
|---|---:|---:|---:|---|
| 武器 | 8 | **90** | +82 | §3 |
| 技能（增益） | 4 | **40** | +36 | §4 |
| 护符 | 10 | **45** | +35 | §5 |
| 消耗品 | 4 | **25** | +21 | §6 |
| **装备合计** | 26 | **200** | **+174** | §2.1（完成度 13.5% → 100%） |
| 普通小怪 | 12 | **36** | +24 | §14.3 |
| 精英 | 6 | **12** | +6 | §14.3 |
| BOSS | 3 | **12** | +9 | §14.3 |
| **敌人合计** | 21 | **60** | **+39** | §14.3（普通36 / 精英12 / BOSS12） |

> 装备 200 件是纯内容工程（新建 .tres，不改代码），分 4 批铺（M1~M4，§12.5）。

### 12.2 装备分类 × 稀有度矩阵（200 = 4 类 × 4 档）
| 分类 | common | uncommon | rare | epic | 小计 | 占比 |
|---|---:|---:|---:|---:|---:|---:|
| 武器 | 36 | 27 | 18 | 9 | **90** | 45.0% |
| 技能 | 16 | 12 | 8 | 4 | **40** | 20.0% |
| 护符 | 18 | 14 | 9 | 4 | **45** | 22.5% |
| 消耗品 | 10 | 8 | 5 | 2 | **25** | 12.5% |
| **合计** | **80** | **61** | **40** | **19** | **200** | 100% |

- 全局稀有度曲线 ≈ **40% / 30% / 20% / 10%** 金字塔（epic 19 件 ≈ 9.5%）；稀有度**只定强度，不定转轮频率**（避免「神装打不出去」）。
- ⚠️ 代码缺口：`ItemData`（护符/消耗品）**尚无 `rarity` 字段**，落地前需给 `item_data.gd` 加 `@export var rarity`（强度分档/商店用，不动结算）。

### 12.3 武器（90）细分
- **元素（6 桶 × 15）**：无 15 / 火 15 / 冰 15 / 毒 15 / 光 15 / 暗 15（均匀，保证每个元素都有对策武器）。
- **原型（4 类）**：轻型 24 / 中型 26 / 重型 22 / 法器 18。
- **稀有度**：36/27/18/9（§12.2）。三维自由组合，9 件 epic 各占元素/原型象限天花板。

### 12.4 敌人（60）规划
单局固定 12 房（每幕 2 普通 + 1 精英 + 1 BOSS）；下列为**内容池**规模，随机抽取：

| 类型 | 每幕池规模 | 抽取/局 | 全游戏总计 |
|---|---:|---:|---:|
| 普通 | 12（4 选 2） | 2/幕 × 3 | **36** |
| 精英 | 4（2 选 1） | 1/幕 × 3 | **12** |
| BOSS | 4（1 固定 + 3 轮替） | 1/幕 × 3 | **12** |

- **BOSS 池每幕 4** = 1 固定（act boss）+ 3 轮替（含秘密 BOSS）；单局遇 3 个，多周目可见不同组合。
- **威胁档分布**（与装备金字塔呼应）：杂兵 trash 23 / 标准 19 / 强敌 14 / 首领·独特 4（epic 4 = 3 秘密 BOSS + 1 独特精英）。
- **行为族组合数学**：8 普通族 + 6 精英族 + BOSS 机制族（护甲熔铸/深渊侵蚀/低语封锁 + 元素使/召唤/狂暴/镜像/终焉），每个族跨元素/幕实例化，避免手搓 60 份 stat block（需先抽离 `EnemyArchetype`）。

### 12.5 落地里程碑（分批铺，每批可独立玩）
| 批次 | 累计装备 | 重点 |
|---|---:|---|
| M1 骨架 | 60 | 补全每类 common + 各元素 ≥1 武器 |
| M2 特化 | 120 | 补齐 uncommon + 元素对策武器成系列 |
| M3 构筑 | 165 | 铺 rare，加入混合护符/独特 special |
| M4 追逐 | 200 | 铺 epic（19 件构筑支点） |

敌人同步：M1 先抽离 `EnemyArchetype` 到 40 敌（24/9/7），M2 满池 60（36/12/12）。

### 12.6 与 §11 的关系
- **§11** 是「立刻解阻塞的最小增量」：武器补 light/dark（跑通 Act2/3 元素克制）+ 护符 3 缺口 + 非 BOSS 房课程化 + 铁砧等级通道 + 商店升级重构（约 7 个新 .tres）。
- **§12** 是「v1.0 完备态」：装备 200（武器90/护符45/技能40/消耗品25）+ 敌人 60，按 M1~M4 分批。
- 两者不冲突：先交 §11 最小增量跑通一局，再按 §12 规模铺满。技能/消耗品在 §11 范围暂不加，但其完整目标量（40/25）在 §12。

