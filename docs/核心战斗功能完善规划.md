# 核心战斗功能完善规划

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | 2026-06-22 | 初始版本，基于Unity原版实现分析 |

---

## 一、现状分析

### 1.1 已有基础系统

| 系统 | 状态 | 说明 |
|------|------|------|
| StateComponent | ✅ 已实现 | HP/能量/体力/热量基础状态管理 |
| WeaponComponent | ✅ 已实现 | 武器切换、攻击冷却 |
| MovementComponent | ✅ 已实现 | 移动、速度控制 |
| StatusEffectComponent | ✅ 已实现 | 状态效果管理 |
| DamageSystem | ✅ 已实现 | 伤害计算、属性克制 |
| PlayerController | ✅ 已实现 | 玩家控制基础 |

### 1.2 Unity原版实现参考

| 功能 | Unity实现 | 关键文件 |
|------|-----------|----------|
| 体力系统 | Stamina.cs | `Assets\_Scripts\Scene\Battle\Unit\Common\Stamina.cs` |
| 热量系统 | HeatSystem.cs | `Assets\_Scripts\Scene\Battle\Unit\Common\HeatSystem.cs` |
| 技能系统 | SkillBase.cs, ISkill.cs | `Assets\_Scripts\Scene\Battle\Unit\Skill\` |
| 弹药系统 | AddAmmoEvent, RestoreAmmoEvent | `Assets\_Scripts\Scene\Battle\Unit\Event\UnitEvent\` |
| 闪避冲刺 | Dash.cs, SkillPluginSprint.cs | `Assets\_Scripts\Scene\Battle\Unit\Fsm\Dash.cs` |

---

## 二、功能模块详细规划

### 模块1：体力与闪避系统

#### 1.1 体力消耗机制

**目标**：实现闪避/冲刺消耗体力，体力不足时限制行动

**实现要点**：

1. **体力消耗规则**
   - 闪避：消耗15体力
   - 冲刺：消耗10体力/秒
   - 体力恢复：10体力/秒（延迟1秒后开始恢复）
   - 体力不足阈值：< 5体力时无法闪避/冲刺

2. **体力状态管理**
   - 正常状态：体力充足，可正常行动
   - 体力不足状态：体力< 5，无法闪避/冲刺
   - 体力耗尽状态：体力=0，进入短暂眩晕

3. **UI反馈**
   - HUD体力条显示
   - 体力不足时体力条闪烁红色
   - 体力耗尽时显示提示文字

**技术实现**：

```gdscript
# 扩展 StateComponent
class_name StaminaSystem
extends Node

signal stamina_changed(current: float, max_value: float)
signal stamina_depleted()
signal stamina_low()

@export var max_stamina: float = 100.0
@export var recover_speed: float = 10.0
@export var recover_delay: float = 1.0

var current_stamina: float
var _recover_timer: float = 0.0
var _is_depleted: bool = false

func consume_stamina(amount: float) -> bool:
    if current_stamina < amount:
        return false
    current_stamina -= amount
    _recover_timer = recover_delay
    stamina_changed.emit(current_stamina, max_stamina)
    return true

func _process(delta: float):
    if _recover_timer > 0:
        _recover_timer -= delta
    else:
        if current_stamina < max_stamina:
            current_stamina += recover_speed * delta
            current_stamina = min(current_stamina, max_stamina)
            stamina_changed.emit(current_stamina, max_stamina)
```

**文件清单**：
- `scripts/battle/stamina_system.gd` - 体力系统核心
- `scripts/components/dodge_component.gd` - 闪避组件
- `scripts/components/sprint_component.gd` - 冲刺组件

#### 1.2 闪避实现

**目标**：实现短距离快速移动，消耗体力，短暂无敌

**实现要点**：

1. **闪避参数**
   - 闪避距离：150像素
   - 闪避时间：0.2秒
   - 闪避冷却：0.5秒
   - 体力消耗：15

2. **闪避行为**
   - 按下闪避键（Shift）触发
   - 向移动方向闪避，无移动时向后方闪避
   - 闪避期间短暂无敌（0.15秒）
   - 闪避结束后有短暂僵直（0.1秒）

3. **视觉效果**
   - 闪避时角色残影
   - 闪避时角色半透明
   - 闪避方向指示

**技术实现**：

```gdscript
class_name DodgeComponent
extends Node

signal dodge_started(direction: Vector2)
signal dodge_finished()

@export var dodge_distance: float = 150.0
@export var dodge_duration: float = 0.2
@export var dodge_cooldown: float = 0.5
@export var stamina_cost: float = 15.0

var _dodge_timer: float = 0.0
var _cooldown_timer: float = 0.0
var _is_dodging: bool = false

func can_dodge() -> bool:
    return _cooldown_timer <= 0.0 and not _is_dodging

func dodge(direction: Vector2):
    if not can_dodge():
        return
    
    var stamina = get_parent().get_node("StaminaSystem")
    if not stamina.consume_stamina(stamina_cost):
        return
    
    _is_dodging = true
    _dodge_timer = dodge_duration
    _cooldown_timer = dodge_cooldown
    
    # 执行闪避移动
    var tween = create_tween()
    var start_pos = get_parent().global_position
    var end_pos = start_pos + direction * dodge_distance
    
    tween.tween_property(get_parent(), "global_position", end_pos, dodge_duration)
    tween.tween_callback(_on_dodge_finished)
    
    dodge_started.emit(direction)

func _process(delta: float):
    if _dodge_timer > 0:
        _dodge_timer -= delta
    if _cooldown_timer > 0:
        _cooldown_timer -= delta

func _on_dodge_finished():
    _is_dodging = false
    dodge_finished.emit()
```

#### 1.3 冲刺实现

**目标**：实现持续加速移动，持续消耗体力

**实现要点**：

1. **冲刺参数**
   - 冲刺速度倍率：1.5倍
   - 体力消耗：10体力/秒
   - 冲刺转向速度：降低30%

2. **冲刺行为**
   - 按住冲刺键（Ctrl）持续冲刺
   - 体力不足时自动停止冲刺
   - 冲刺时转向速度降低

**技术实现**：

```gdscript
class_name SprintComponent
extends Node

signal sprint_started()
signal sprint_finished()

@export var speed_multiplier: float = 1.5
@export var stamina_cost_per_second: float = 10.0

var _is_sprinting: bool = false

func start_sprint():
    if _is_sprinting:
        return
    
    var stamina = get_parent().get_node("StaminaSystem")
    if stamina.current_stamina < stamina_cost_per_second:
        return
    
    _is_sprinting = true
    get_parent().get_node("MovementComponent").speed_multiplier = speed_multiplier
    sprint_started.emit()

func stop_sprint():
    if not _is_sprinting:
        return
    
    _is_sprinting = false
    get_parent().get_node("MovementComponent").reset_speed_multiplier()
    sprint_finished.emit()

func _process(delta: float):
    if _is_sprinting:
        var stamina = get_parent().get_node("StaminaSystem")
        if not stamina.consume_stamina(stamina_cost_per_second * delta):
            stop_sprint()
```

---

### 模块2：武器过热系统

#### 2.1 热量累积机制

**目标**：攻击累积热量，热量过高时武器性能下降

**实现要点**：

1. **热量参数**
   - 最大热量：100
   - 冷却速度：8热量/秒
   - 预热阈值：75热量（进入过热警告）
   - 过热阈值：100热量（强制冷却）

2. **热量累积**
   - 每次攻击增加热量（基于武器类型）
   - 近战武器：5热量/次
   - 远程武器：8热量/次
   - 攻击后延迟1秒开始冷却

3. **过热效果**
   - 预热状态（75-99热量）：攻击速度降低20%
   - 过热状态（100热量）：无法攻击，强制冷却5秒
   - 过热警告：热量条闪烁橙色

**技术实现**：

```gdscript
class_name HeatSystem
extends Node

signal heat_changed(current: float, max_value: float)
signal overheat_warning()
signal overheat_triggered()
signal overheat_cleared()

@export var max_heat: float = 100.0
@export var cooling_speed: float = 8.0
@export var warning_threshold: float = 75.0
@export var overheat_threshold: float = 100.0

var current_heat: float = 0.0
var _cooling_timer: float = 0.0
var _is_overheated: bool = false

func add_heat(amount: float):
    if _is_overheated:
        return
    
    current_heat += amount
    current_heat = min(current_heat, max_heat)
    _cooling_timer = 1.0  # 攻击后延迟冷却
    heat_changed.emit(current_heat, max_heat)
    
    if current_heat >= overheat_threshold:
        _trigger_overheat()
    elif current_heat >= warning_threshold:
        overheat_warning.emit()

func _trigger_overheat():
    _is_overheated = true
    current_heat = max_heat
    overheat_triggered.emit()
    
    # 强制冷却5秒
    var tween = create_tween()
    tween.tween_interval(5.0)
    tween.tween_callback(_clear_overheat)

func _clear_overheat():
    _is_overheated = false
    current_heat = 0.0
    overheat_cleared.emit()

func _process(delta: float):
    if _cooling_timer > 0:
        _cooling_timer -= delta
    else:
        if current_heat > 0 and not _is_overheated:
            current_heat -= cooling_speed * delta
            current_heat = max(current_heat, 0.0)
            heat_changed.emit(current_heat, max_heat)

func get_heat_ratio() -> float:
    return current_heat / max_heat if max_heat > 0 else 0.0

func is_overheated() -> bool:
    return _is_overheated

func is_warning() -> bool:
    return current_heat >= warning_threshold and current_heat < overheat_threshold
```

#### 2.2 武器性能影响

**目标**：热量影响武器攻击速度和伤害

**实现要点**：

1. **攻击速度影响**
   - 正常状态：100%攻击速度
   - 预热状态：80%攻击速度
   - 过热状态：0%攻击速度（无法攻击）

2. **伤害影响**
   - 正常状态：100%伤害
   - 预热状态：90%伤害
   - 过热状态：0%伤害

**技术实现**：

```gdscript
# 在 WeaponComponent 中扩展
func get_attack_speed_modifier() -> float:
    var heat = get_parent().get_node("HeatSystem")
    if heat.is_overheated():
        return 0.0
    elif heat.is_warning():
        return 0.8
    else:
        return 1.0

func get_damage_modifier() -> float:
    var heat = get_parent().get_node("HeatSystem")
    if heat.is_overheated():
        return 0.0
    elif heat.is_warning():
        return 0.9
    else:
        return 1.0

func attack():
    if not can_attack():
        return
    
    var heat = get_parent().get_node("HeatSystem")
    if heat.is_overheated():
        print("武器过热，无法攻击！")
        return
    
    # 计算修正后的攻击速度
    var modified_attack_speed = current_weapon.attack_speed * get_attack_speed_modifier()
    attack_cooldown = 1.0 / modified_attack_speed
    
    # 计算修正后的伤害
    var modified_damage = current_weapon.damage * get_damage_modifier()
    
    # 执行攻击逻辑...
```

---

### 模块3：弹药系统

#### 3.1 弹药管理

**目标**：远程武器需要弹药，弹药耗尽无法攻击

**实现要点**：

1. **弹药参数**
   - 弹药容量：30发（默认）
   - 备弹：无限（简化版本）
   - 装弹时间：2秒
   - 装弹消耗：0（简化版本）

2. **弹药行为**
   - 每次射击消耗1发弹药
   - 弹药耗尽时自动装弹
   - 手动装弹（按R键）
   - 装弹期间无法攻击

3. **弹药补给**
   - 弹药箱：补充30发弹药
   - 商店购买弹药

**技术实现**：

```gdscript
class_name AmmoSystem
extends Node

signal ammo_changed(current: int, max_capacity: int)
signal reload_started()
signal reload_finished()
signal ammo_empty()

@export var max_ammo: int = 30
@export var reload_time: float = 2.0

var current_ammo: int
var _is_reloading: bool = false
var _reload_timer: float = 0.0

func _ready():
    current_ammo = max_ammo

func consume_ammo() -> bool:
    if _is_reloading:
        return false
    
    if current_ammo <= 0:
        ammo_empty.emit()
        start_reload()
        return false
    
    current_ammo -= 1
    ammo_changed.emit(current_ammo, max_ammo)
    
    if current_ammo <= 0:
        ammo_empty.emit()
        start_reload()
    
    return true

func start_reload():
    if _is_reloading:
        return
    
    _is_reloading = true
    _reload_timer = reload_time
    reload_started.emit()

func _process(delta: float):
    if _is_reloading:
        _reload_timer -= delta
        if _reload_timer <= 0:
            current_ammo = max_ammo
            _is_reloading = false
            ammo_changed.emit(current_ammo, max_ammo)
            reload_finished.emit()

func add_ammo(amount: int):
    current_ammo = min(current_ammo + amount, max_ammo)
    ammo_changed.emit(current_ammo, max_ammo)

func get_ammo_ratio() -> float:
    return float(current_ammo) / float(max_ammo) if max_ammo > 0 else 0.0
```

#### 3.2 武器弹药集成

**目标**：远程武器集成弹药系统

**实现要点**：

1. **武器弹药配置**
   - 每种武器配置弹药容量
   - 每次射击消耗弹药数量

2. **UI显示**
   - HUD显示当前弹药/最大弹药
   - 弹药不足时弹药条闪烁红色
   - 装弹时显示装弹进度

**技术实现**：

```gdscript
# 在 WeaponComponent 中扩展
func _ranged_attack():
    var ammo = get_parent().get_node("AmmoSystem")
    if not ammo.consume_ammo():
        return
    
    # 原有的远程攻击逻辑...
```

---

### 模块4：技能系统（DMD 路线）

**设计原则**：技能不硬编码，由装备产出。玩家通过装备 Build 决定技能组。

| 维度 | 说明 |
|------|------|
| 技能来源 | EquipmentBase.granted_skill 决定每个装备携带的主动技能 |
| 技能槽 | 最多 4 个主动技能槽（1/2/3/4），由装备物品决定 |
| 注册方式 | 装备时 EquipmentManager 将 `item.granted_skill` 注册到 SkillManager；卸下时移除 |
| 角色固有 | EscapeSkill（Z 键，引导5s撤离，90s CD），不占技能槽 |
| 被动效果 | 词缀的 StatModifier/ConditionalBonus 直接作用于角色，不占技能槽 |
| 构筑导向 | 技能是对装备 synergy 的补充，不是独立的"选技能"系统 |

#### 当前实现状态

| 项目 | 状态 |
|------|------|
| SkillBase / SkillManager 框架 | ✅ |
| SkillManager add_or_upgrade + 替换信号 | ✅ |
| SkillBase level 字段（1-4级） | ✅ |
| SkillPickup 光球（脚本+碰撞+拾取） | ✅ |
| 技能栏 UI（4槽 + 等级 + 冷却遮罩 + 能量变暗） | ✅ |
| 升级浮动文字 "+1 Lv.X!" | ✅ |
| 替换选择弹窗 | ✅ |
| 房间清空技能掉落（50%概率） | ✅ |
| 输入映射 1/2/3/4 + Z（Escape） | ✅ |
| EscapeSkill 独立不占槽 | ✅ |
| 旧 DMD 桥接代码清理 | ✅ Phase B |

#### 技能基类

**SkillBase**（`scripts/battle/skill_base.gd`）：
- `@export skill_name / skill_description / energy_cost / cooldown / duration`
- `can_use(state: StateComponent) -> bool`：检查冷却和能量
- `use(user: Node2D) -> bool`：消耗能量，开始冷却，调用 `_activate_skill`
- `tick(delta)`：冷却计时
- `get_cooldown_ratio() -> float`

**SkillManager**（`scripts/battle/skill_manager.gd`）：
- `skills: Array[SkillBase]`
- `add_skill(skill)` / `remove_skill(skill)` / `use_skill(index, user)`
- 信号 `skill_used(index, skill)`

#### 技能注册流程

```
玩家拾取/装备物品
  → EquipmentManager.equip(item)
    → _apply_item(item)  // 应用词缀 stat/trigger/condition
    → 检查 item.granted_skill
      → SkillManager.add_skill(granted_skill)  // 注册到技能槽
  → 技能出现在技能栏（1/2/3/4）

玩家卸下/替换物品
  → EquipmentManager.unequip(slot)
    → _remove_item(item)
    → SkillManager.remove_skill(granted_skill)  // 从技能槽移除
  → 技能从技能栏消失
```

#### 技能栏 UI

位置：底部居中 `(250, 324)`，4 槽 32×32，间距 4px
- 快捷键标签（1/2/3/4）
- 技能名 Label
- 冷却遮罩（黑色半透明，实时更新高度）
- 能量不足时槽位变暗

---

## 三、UI界面规划

### 3.1 HUD界面扩展

**目标**：在现有HUD基础上添加新功能显示

**新增元素**：

1. **体力条**
   - 位置：HP条下方
   - 颜色：绿色
   - 闪烁：体力不足时闪烁红色

2. **热量条**
   - 位置：能量条右侧
   - 颜色：橙色
   - 闪烁：过热警告时闪烁

3. **弹药显示**
   - 位置：武器信息下方
   - 格式：当前弹药/最大弹药
   - 闪烁：弹药不足时闪烁红色

4. **技能栏**
   - 位置：屏幕底部中央
   - 4个技能图标
   - 冷却遮罩
   - 快捷键提示

**技术实现**：

```gdscript
# 在 HUD.gd 中扩展
func _build_extended_bars():
    # 体力条
    _build_stamina_bar()
    
    # 热量条
    _build_heat_bar()
    
    # 弹药显示
    _build_ammo_display()
    
    # 技能栏
    _build_skill_bar()

func _build_stamina_bar():
    var stamina_bg = ColorRect.new()
    stamina_bg.name = "StaminaBg"
    stamina_bg.position = Vector2(10, 90)
    stamina_bg.size = Vector2(200, 12)
    stamina_bg.color = Color(0.15, 0.15, 0.15, 0.8)
    add_child(stamina_bg)
    
    var stamina_fill = ColorRect.new()
    stamina_fill.name = "StaminaFill"
    stamina_fill.position = Vector2(10, 90)
    stamina_fill.size = Vector2(200, 12)
    stamina_fill.color = Color(0.15, 0.7, 0.15)
    add_child(stamina_fill)
    
    var stamina_label = Label.new()
    stamina_label.name = "StaminaLabel"
    stamina_label.position = Vector2(14, 88)
    stamina_label.add_theme_color_override("font_color", Color(1, 1, 1))
    stamina_label.text = "体力: --/--"
    add_child(stamina_label)
    
    _bars["stamina"] = {bg = stamina_bg, fill = stamina_fill, label = stamina_label, max_w = 200}

func _build_heat_bar():
    var heat_bg = ColorRect.new()
    heat_bg.name = "HeatBg"
    heat_bg.position = Vector2(220, 34)
    heat_bg.size = Vector2(100, 16)
    heat_bg.color = Color(0.15, 0.15, 0.15, 0.8)
    add_child(heat_bg)
    
    var heat_fill = ColorRect.new()
    heat_fill.name = "HeatFill"
    heat_fill.position = Vector2(220, 34)
    heat_fill.size = Vector2(100, 16)
    heat_fill.color = Color(0.9, 0.6, 0.1)
    add_child(heat_fill)
    
    var heat_label = Label.new()
    heat_label.name = "HeatLabel"
    heat_label.position = Vector2(224, 32)
    heat_label.add_theme_color_override("font_color", Color(1, 1, 1))
    heat_label.text = "热量: 0%"
    add_child(heat_label)
    
    _bars["heat"] = {bg = heat_bg, fill = heat_fill, label = heat_label, max_w = 100}

func _build_ammo_display():
    var ammo_label = Label.new()
    ammo_label.name = "AmmoLabel"
    ammo_label.position = Vector2(10, 110)
    ammo_label.add_theme_color_override("font_color", Color(1, 1, 1))
    ammo_label.text = "弹药: --/--"
    add_child(ammo_label)
    
    _bars["ammo"] = {label = ammo_label}

func _build_skill_bar():
    var skill_bar = Control.new()
    skill_bar.name = "SkillBar"
    skill_bar.position = Vector2(300, 550)
    skill_bar.size = Vector2(200, 40)
    add_child(skill_bar)
    
    for i in range(4):
        var skill_slot = ColorRect.new()
        skill_slot.name = "SkillSlot%d" % i
        skill_slot.position = Vector2(i * 50, 0)
        skill_slot.size = Vector2(40, 40)
        skill_slot.color = Color(0.3, 0.3, 0.3, 0.8)
        skill_bar.add_child(skill_slot)
        
        var skill_key = Label.new()
        skill_key.text = ["Q", "W", "E", "R"][i]
        skill_key.position = Vector2(15, 25)
        skill_key.add_theme_color_override("font_color", Color(1, 1, 1))
        skill_slot.add_child(skill_key)
        
        var skill_cooldown = ColorRect.new()
        skill_cooldown.name = "Cooldown"
        skill_cooldown.position = Vector2(0, 0)
        skill_cooldown.size = Vector2(40, 40)
        skill_cooldown.color = Color(0, 0, 0, 0.7)
        skill_slot.add_child(skill_cooldown)
```

---

## 四、实施步骤

### 阶段1：体力与闪避系统（3-4天）

**Day 1-2：体力系统基础**
- [ ] 创建 `StaminaSystem` 类
- [ ] 实现体力消耗和恢复逻辑
- [ ] 添加体力状态管理
- [ ] HUD体力条显示

**Day 3-4：闪避与冲刺**
- [ ] 创建 `DodgeComponent` 类
- [ ] 实现闪避逻辑和无敌帧
- [ ] 创建 `SprintComponent` 类
- [ ] 实现冲刺逻辑
- [ ] 集成体力消耗

### 阶段2：武器过热系统（2-3天）

**Day 1-2：热量系统基础**
- [ ] 创建 `HeatSystem` 类
- [ ] 实现热量累积和冷却逻辑
- [ ] 添加过热状态管理
- [ ] HUD热量条显示

**Day 3：武器性能影响**
- [ ] 修改 `WeaponComponent` 集成热量系统
- [ ] 实现攻击速度和伤害修正
- [ ] 添加过热警告效果

### 阶段3：弹药系统（2天）

**Day 1：弹药管理**
- [ ] 创建 `AmmoSystem` 类
- [ ] 实现弹药消耗和装弹逻辑
- [ ] 添加弹药补给机制

**Day 2：武器集成**
- [ ] 修改 `WeaponComponent` 集成弹药系统
- [ ] HUD弹药显示
- [ ] 弹药箱实现

### 阶段4：技能系统（DMD 路线）

> **当前状态**：SkillBase/SkillManager 框架 ✅，技能栏 UI ✅，输入映射 ✅
> **范式变更**：技能由装备产出（EquipmentBase.granted_skill），非硬编码

**Phase A — 独立拾取+升星（1天）**
- [x] SkillManager 重写为 add_or_upgrade + 替换信号
- [x] SkillBase 加 level 字段（1-4级）
- [x] SkillPickup 光球（Area2D脚本+碰撞+拾取检测）
- [x] HUD 技能栏等级小数字
- [x] 升级浮动文字 + 替换选择弹窗
- [x] 房间清空奖励 50% 概率技能光球

**Phase B — 清理旧绑定 ✅**
- [x] 删除 EquipmentBase/Affix/SetBonus 所有 skill 字段
- [x] 删除 EquipmentManager/SetBonusManager 技能桥接
- [x] 删除 ACTIVATE_SKILL 分支+枚举
- [x] 删除 EquipmentDrop._try_grant_skill
- [x] 删除 SkillManager add/remove_skill 兼容方法

**Phase C — 扩展（延伸）**
- [x] 新增词缀技能效果 EffectAction
- [x] 套装奖励产出技能
- [ ] 技能槽拖拽排序
- [ ] 快捷键配置 UI

### 阶段5：测试与优化（2-3天）

**Day 1-2：功能测试**
- [ ] 体力系统测试
- [ ] 热量系统测试
- [ ] 弹药系统测试
- [ ] 技能系统测试

**Day 3：优化与修复**
- [ ] 性能优化
- [ ] Bug修复
- [ ] 平衡性调整

---

## 五、技术难点与解决方案

### 5.1 体力系统与状态机集成

**难点**：体力耗尽需要与角色状态机集成，进入特殊状态

**解决方案**：
- 在 `StateComponent` 中添加体力耗尽状态
- 使用信号机制通知状态变化
- 状态机监听体力变化信号

### 5.2 热量系统与武器系统耦合

**难点**：热量需要影响武器性能，但又要保持系统解耦

**解决方案**：
- 使用组件化设计，热量系统和武器系统独立
- 通过信号和查询接口进行通信
- 武器系统查询热量状态来修正性能

### 5.3 技能系统的扩展性

**难点**：技能系统需要支持多种不同类型的技能

**解决方案**：
- 使用继承和多态，`SkillBase` 作为基类
- 每个具体技能继承 `SkillBase` 并实现 `_activate_skill`
- 使用资源文件配置技能参数

### 5.4 UI实时更新

**难点**：多个系统状态需要实时更新UI

**解决方案**：
- 使用信号机制，状态变化时发送信号
- UI监听信号并更新显示
- 使用缓动动画平滑过渡

---

## 六、配置数据结构

### 6.1 体力配置

```json
{
  "stamina": {
    "max_stamina": 100.0,
    "recover_speed": 10.0,
    "recover_delay": 1.0,
    "dodge_cost": 15.0,
    "sprint_cost_per_second": 10.0,
    "low_threshold": 5.0
  }
}
```

### 6.2 热量配置

```json
{
  "heat": {
    "max_heat": 100.0,
    "cooling_speed": 8.0,
    "warning_threshold": 75.0,
    "overheat_threshold": 100.0,
    "melee_heat_per_attack": 5.0,
    "ranged_heat_per_attack": 8.0,
    "overheat_duration": 5.0
  }
}
```

### 6.3 弹药配置

```json
{
  "ammo": {
    "default_capacity": 30,
    "reload_time": 2.0,
    "ammo_box_amount": 30
  }
}
```

### 6.4 技能配置

技能参数通过 `.tres` Resource 文件配置，位于 `resources/skills/`：

| 文件 | 技能 | 参数 |
|------|------|------|
| `heal_skill.tres` | 治疗 | energy_cost=30, cooldown=10, heal_percent=0.5 |
| `shockwave_skill.tres` | 震波 | energy_cost=10, cooldown=8, aoe_range=120, damage_mult=1.5 |
| `escape_skill.tres` | 紧急撤离 | energy_cost=0, cooldown=90 |

技能通过 `EquipmentBase.granted_skill` 字段挂载到装备上，由 `EquipmentManager` 在装备/卸下时注册/移除。

---

## 七、文件清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `scripts/battle/stamina_system.gd` | ✅ 体力系统核心 |
| `scripts/components/dodge_component.gd` | ✅ 闪避组件 |
| `scripts/components/sprint_component.gd` | ✅ 冲刺组件 |
| `scripts/battle/heat_system.gd` | ✅ 热量系统核心 |
| `scripts/battle/ammo_system.gd` | ✅ 弹药系统核心 |
| `scripts/battle/skill_base.gd` | ✅ 技能基类 |
| `scripts/battle/skill_manager.gd` | ✅ 技能管理器 |
| `scripts/battle/skills/heal_skill.gd` | ✅ 治疗技能 |
| `scripts/battle/skills/shockwave_skill.gd` | ✅ 震波技能 |
| `scripts/battle/skills/escape_skill.gd` | ✅ 紧急撤离 |
| `scripts/battle/skills/shield_skill.gd` | ❌ 护盾技能（待实现） |
| `scripts/battle/skills/dash_skill.gd` | ❌ 冲刺技能（待实现） |
| `resources/skills/` | ❌ 技能 Resource 配置目录（待创建） |
| `resources/config/stamina_config.json` | 体力配置 |
| `resources/config/heat_config.json` | 热量配置 |
| `resources/config/ammo_config.json` | 弹药配置 |

### 修改文件（已完成）

| 文件路径 | 修改内容 |
|----------|----------|
| `scripts/battle/state_component.gd` | ✅ 集成体力系统 |
| `scripts/battle/player_controller.gd` | ✅ 集成闪避、冲刺、技能、弹药 |
| `scripts/components/weapon_component.gd` | ✅ 集成热量和弹药系统 |
| `scenes/ui/hud.gd` | ✅ 扩展UI显示（含技能栏待补全） |

---

## 八、测试计划

### 8.1 单元测试

| 测试项 | 测试内容 |
|--------|----------|
| 体力系统 | 体力消耗、恢复、状态切换 |
| 热量系统 | 热量累积、冷却、过热状态 |
| 弹药系统 | 弹药消耗、装弹、补给 |
| 技能系统 | 技能释放、冷却、消耗 |

### 8.2 集成测试

| 测试项 | 测试内容 |
|--------|----------|
| 战斗循环 | 完整战斗流程测试 |
| 系统交互 | 各系统之间的交互测试 |
| UI显示 | 实时状态显示测试 |
| 性能测试 | 帧率和内存占用测试 |

---

**文档版本**: v3.0  
**创建日期**: 2026-06-22  
**最后更新**: 2026-06-30（DMD 路线对齐）  
**预计工期**: 12-16天  
**参考文档**: 《游戏策划文档.md》、《项目实施规划.md》、《session_handoff_20260630.md》