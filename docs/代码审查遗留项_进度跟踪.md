# 代码审查遗留项 · 进度跟踪

> 依据：2026-08-07 对 `duel_controller.gd`（拆分方案 B 重构后）的全面代码审查。
> 覆盖：子系统层（scripts/systems/）、controller 层、UI/可视化层、遗留专项（死代码）四个维度。
> 状态标记：`✅ 已完成` / `🔄 进行中` / `⏳ 未开始`。每步改动后须 headless 校验 + F6 实测再更新状态。

## 1. 审查结论摘要

- 架构方向正确：组合注入 + 薄转发，无 autoload/全局注册表（符合项目铁律）。
- 已从 2252 行单体拆出 5 个 RefCounted 子系统（MetaStore/AnvilSystem/ShopSystem/RewardSystem/LoadoutSystem）。
- 主要剩余债务：**代码手写 UI 残留**、**战斗核心 _evaluate 单体**、**类型/风格收尾**（泛型、has_method 等）。
- 已完成：死代码/死键清理、UI 信号化与语义入口、奖励/商店/金币升级数值资源化（进度见 §2）。

## 2. 已完成项（✅）

| ID | 内容 | 提交 |
|---|---|---|
| B1 | 修复 3 个 BOSS 房 `boss_relic_path`（指向不存在的 `resources/items/` → `resources/charms/`） | `0e5691b` |
| B3 | `TEST_SMALL_OWNED` 默认 true→false；`TEST_*` 去前缀为正式平衡值 | `0e5691b` |
| T1 | `DuelController` 加 `class_name`；5 子系统 `_ctrl: DuelController` + `_init(ctrl: DuelController)` 标类型 | `0e5691b` |
| B2 | `logs` 归 `battle_hud` 自持，删除 controller/battle_state 的 logs，消除快照穿透写 | `50d98e8` |
| H2 | 三个 `_on_reward_*` 合并为 `_finish_room(apply_fn, is_boss)` | `50d98e8` |
| V1 | 新建 `item_card.tscn` + `item_card.gd`，消灭 battle_hud 六份重复卡片构造器（净减 88 行） | `50d98e8` |
| D1-D9 | 死代码清理：KPI/WEAPON_STEP/UI_BUTTON/`_buff_*` 包装/`_gold_upgrade_*` 转发/cleared 分支/fill_random 参数/pos 参数/busy 双写 | `9aea064` |
| D11 | HUD 死函数删除：`_player_panel_anchor`/`_enemy_panel_anchor`/`_refresh_anvil` | `9aea064` |
| N2 | 空注释桩清理（UI 引用/符号图例/乱权悬注/f(kind) 重复说明） | `9aea064` |
| D10 | meta 6 个已退役死键（weapon_upgrades/weapon_base_bonus/weapon_hit_bonus/charm_upgrades/interference_resist/first_clears）全链路清理 + CHARM_MULT_STEP 删除 | `5ed5d1f` |
| M9/H5 | `game_state` 字符串字面量 → `enum FlowState { PLAYING, WON, LOST }`（controller/battle_state/HUD 同步） | `5ed5d1f` |
| R3 | 铁砧屏不再穿透子系统私有方法：AnvilSystem 新增公开查询 `collection_info()`，anvil_screen 删除 4 处 `_anvil_*` 私有调用 | `c25c58a` |
| H3 | 奖励屏改走语义入口：controller 新增 `_open_reward_screen(is_boss)`，reward_screen 不再直写 controller 字段、改从 state 快照读 | `f9fe880` |
| M1 | 7 处 UI 直绑 controller 私有方法信号化：新增 card_toggled/meta_choice_chosen/gold_upgrade_requested/overlay_button_pressed/consumable_used/shop_leave_requested/anvil_back_requested 信号 | `b430489` |
| D1 | 奖励效果数值资源化：`RewardData.value` + 8 个 rewards/*.tres 补值，apply_reward 改读资源 | `9913a66` |
| D3 | 商店经济配置化：新增 `ShopConfig` Resource + shop_config.tres（价格表/jitter/下限/返现比） | `6728a0e` |
| M7 | 金币升级定义资源化：新增 `GoldUpgradeDef` Resource + 4 个 .tres，ShopSystem 扫描收集，删除 controller 内联 const | `3a4404b` |
| M2 | `_start_room` 的 ante 难度曲线数学抽为 `_ante_scale(r, idx)` 纯函数 | `bd24f68` |
| M3 | `_on_spin_pressed` 的 special 连锁重触发循环抽为 `_chain_loop()` | `bd24f68` |
| N5 | 商店转发统一 `_after_shop_change()`（买卖/金币升级共用 UI 刷新出口） | `bd24f68` |
| N4 | `_refresh_consumable_panel` 删除 `has_method` 防御式写法（风格统一） | `bd24f68` |
| M10 | 数组泛型化统一（池/勾选/腰带/转轮数组，扫描结果 assign() 显式转换；reward_choices 混合元素保持未泛型） | `bd24f68` |
| V2 | 各屏按钮/页签/卡片进 .tscn：房间歇态两按钮（battle_hud.tscn）、商店三页签+关闭（shop_screen.tscn）、铁砧摇/返回 + 三格卡片抽 anvil_cell.tscn 组件（消灭三份重复构造器，修复 radius 矛盾） | `67ddd0f` |

## 3. 未完成项（⏳）

> 中风险（3.1）已全部完成 ✅。UI 可视化剩余 V5（主题扩充）。低优先级见 3.3。

### 3.2 高风险/高收益（UI 可视化）

- [ ] **V5** 主题可视化：扩充 `theme/battle_theme.tres` + `theme_type_variation`（TitleLabel/CardTitle/PriceLabel 等），删除各 screen `configure()` 内 73 处 `add_theme_*_override`（注意：元素高亮/飘字等动态色须留代码）

### 3.3 低优先级

- [ ] **H1** `_evaluate`（duel_controller.gd:1437-1593，156 行）拆为 `_settle_symbols/_settle_defense/_settle_counter/_settle_attack`（方案 B 标注"可选"）
- [ ] **D2** anvil drip 数值硬编码（anvil_system.gd:58-63 的 5/3/1）参数化
- [ ] **D4** `ItemData` 缺 `rarity` 字段（护符/消耗品无法分稀有度档，docs §12.2 点名）
- [ ] **L1** `_build_strips`/`_build_pool` 拆 per-item 段纯函数
- [ ] **L2** `state` getter 每次访问重建快照 → 缓存 + 变更失效（性能债，低优先）
- [ ] **L3** 消耗品腰带数据归属跨 controller/ShopSystem 两处 → 可选抽 `ConsumableSystem`
- [ ] **L4** 转轮引擎 5 函数靠共享变量协作 → 可选抽 `ReelEngine`
- [ ] **N1** 30+ 处 M3/M4/M5/Phase C/D/G/S10/T6 历史标记注释清理（注释风格层面）
- [ ] **杂项**：`roll_boss_rewards(room)` 参数标类型+null 校验；`roll_meta_choices` 单候选伪三选一清理；`element_counter` relation/multiplier 重复判定合并；子系统 `_ctrl.hud` 空校验统一；硬编码色/字号清理（battle_hud.gd:813 等）；`ui_panel.tscn`+`_make_side_panel` 死代码删除；`screen.gd` 退役（overlay 场景化后）；飘字池 diff 刷新

## 4. 已完成但需回归验证的项

> 下列改动已提交并通过 headless 解析，**尚未在游戏内 F6 实测**，建议完整跑一局回归：
> 整备勾选 → 商店买卖/金币升级 → 战斗（三连/连锁/克制）→ 精英补给 → BOSS 战利品（含信物）→ 通关元进度三选一 → 死亡回整备。

- [ ] B1（BOSS 信物）— 打 3 个 BOSS 验证信物可拾取
- [ ] B2（logs）— 战斗日志显示正常
- [ ] V1（ItemCard）— 商店/奖励/元进度/金币升级卡片显示正常
- [ ] D10（meta 死键）— 旧存档兼容：删除 `user://lobby_data.json` 后重进无报错
- [ ] R3（铁砧屏查询）— 铁砧打开/摇动/三连动画/图鉴% 显示正常
- [ ] H3（奖励语义入口）— 普通/精英/BOSS 三选一正常弹出，选后推进房间
- [ ] M1（信号化）— 整备勾选/元进度选择/金币升级/失败弹层/消耗品/商店关闭/铁砧返回 均可点击生效
- [ ] D1（奖励数值）— 各奖励效果数值与 .tres 一致（治疗 35% / 护盾 +15 / 金币 +18 等）
- [ ] D3（商店价格）— 商店价格/卖出返现与原手感一致（武器 8+5/级、返现 50%）
- [ ] M7（金币升级）— 4 项升级价格阶梯（6+4/级 等）与满级上限正常

## 5. 涉及文件索引

| 文件 | 说明 |
|---|---|
| `scripts/battle/duel_controller.gd` | 主控制器（现 ~1630 行：编排 + 薄转发 + 战斗核心） |
| `scripts/battle/battle_state.gd` | 只读快照（game_state 已 enum 化） |
| `scripts/systems/*.gd` | 5 个子系统 + ShopConfig/GoldUpgradeDef 配置资源类 |
| `scripts/ui/battle_hud.gd` | 主 HUD（logs 自持；六卡片构造器已组件化；意图信号 16 个） |
| `scripts/ui/item_card.gd` + `scenes/ui/item_card.tscn` | ItemCard 组件（V1 产物） |
| `scripts/ui/anvil_screen.gd` | 铁砧屏（R3 已收敛查询；V2 仍待做） |
| `scripts/ui/shop_screen.gd` / `reward_screen.gd` | 商店/奖励屏（M1/H3 已完成信号化与语义入口） |
| `scripts/utils/resource_scan.gd` | 资源目录扫描（数据驱动基座） |
| `resources/config/` | 数据驱动配置目录：shop_config.tres + gold_upgrades/（D3/M7 产物） |
| `theme/battle_theme.tres` | 主题（V5 扩充目标，当前仅 1 个样式） |
